<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[Hacking to the gate!]]></title>
  <subtitle><![CDATA[为了证明并非止步不前，为了安慰被神牛震慑的心。]]></subtitle>
  <link href="/atom.xml" rel="self"/>
  <link href="http://blog.bipedalbit.net/"/>
  <updated>2015-10-08T13:39:19.424Z</updated>
  <id>http://blog.bipedalbit.net/</id>
  
  <author>
    <name><![CDATA[Bipedal Bit]]></name>
    <email><![CDATA[hmemoryl@163.com]]></email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title><![CDATA[C++ cin、cout的优化及测评]]></title>
    <link href="http://blog.bipedalbit.net/2015/10/8/C-cin%E3%80%81cout%E7%9A%84%E4%BC%98%E5%8C%96%E5%8F%8A%E6%B5%8B%E8%AF%84/"/>
    <id>http://blog.bipedalbit.net/2015/10/8/C-cin、cout的优化及测评/</id>
    <published>2015-10-08T11:56:46.000Z</published>
    <updated>2015-10-08T13:39:19.424Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 参加过ACM比赛和部分自己做过测评的人想必不难发现，C++的cin和cout虽然比起C的scanf和printf来方便很多，但是速度似乎会打很大折扣。于是不求甚解的人们一拍脑门，认定因为C更“底层”，封装程度更低，所以势必比C++的输入输出执行效率更高。然而如若C++有知，恐怕要苦笑了。因为C++正是为了兼容C的输入输出才要在cin、cout中进行指针同步，降低了执行效率。那么可能挽回这种效率损失吗？答案是可以。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; C++中提供了解除指针同步的方法：<code>ios::sync_with_stdio(false)</code>。如字面意思，这个方法用来设置输入输出流是否与C的stdio同步。那么解除同步之后能有多大效果呢？毕竟C++确实因为封装程度比C高多多少少损失了一些执行效率。我动手进行了一系列测评，下面是源码。</p>
<p>&nbsp;&nbsp;&nbsp; 这里我统一对输入输出做了重定向，不论是输入数据来源还是输入数据目标都是“data”文件。所有的输入/输出测试都是读或写10^7次字符串与整型数的组合数据，每种输入/输出测试都进行10次并取平均耗时作为评估值。</p>
<p>&nbsp;&nbsp;&nbsp; 首先是C的输入输出。</p>
<p>&nbsp;&nbsp;&nbsp; scanf.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    freopen(<span class="string">"data"</span>, <span class="string">"r"</span>, <span class="built_in">stdin</span>);
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="keyword">char</span> s[<span class="number">20</span>];
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">scanf</span>(<span class="string">"%s"</span>, s);
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">printf</span>(<span class="string">"average cost: %lfs\n"</span>, t_sum/testCnt);
    fclose(<span class="built_in">stdin</span>);
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; printf.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    freopen(<span class="string">"data"</span>, <span class="string">"w"</span>, <span class="built_in">stdout</span>);
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">printf</span>(<span class="string">"string%d\n"</span>, i);
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">printf</span>(<span class="string">"average cost: %lfs\n"</span>, t_sum/testCnt);
    fclose(<span class="built_in">stdout</span>);
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 然后是不解锁同步的cin、cout方法。</p>
<p>&nbsp;&nbsp;&nbsp; cin.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    <span class="function">ifstream <span class="title">fin</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    streambuf *_cin =  <span class="built_in">cin</span>.rdbuf(fin.rdbuf());
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="built_in">string</span> s;
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">cin</span> &gt;&gt; s;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    <span class="built_in">cin</span>.rdbuf(_cin);
    fin.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; cout.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    <span class="function">ofstream <span class="title">fout</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    streambuf *_cout =  <span class="built_in">cout</span>.rdbuf(fout.rdbuf());
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">cout</span> &lt;&lt; <span class="string">"string"</span> &lt;&lt; i &lt;&lt; <span class="string">'\n'</span>;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    <span class="built_in">cout</span>.rdbuf(_cout);
    fout.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 接下来是解锁同步的cin、cout方法。</p>
<p>&nbsp;&nbsp;&nbsp; optimized_cin.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    ios::sync_with_stdio(<span class="literal">false</span>);
    <span class="function">ifstream <span class="title">fin</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    streambuf *_cin =  <span class="built_in">cin</span>.rdbuf(fin.rdbuf());
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="built_in">string</span> s;
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">cin</span> &gt;&gt; s;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    <span class="built_in">cin</span>.rdbuf(_cin);
    fin.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; optimized_cout.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    ios::sync_with_stdio(<span class="literal">false</span>);
    <span class="function">ofstream <span class="title">fout</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    streambuf *_cout =  <span class="built_in">cout</span>.rdbuf(fout.rdbuf());
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            <span class="built_in">cout</span> &lt;&lt; <span class="string">"string"</span> &lt;&lt; i &lt;&lt; <span class="string">'\n'</span>;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    <span class="built_in">cout</span>.rdbuf(_cout);
    fout.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 同时测试C++直接读写文件的方法。</p>
<p>&nbsp;&nbsp;&nbsp; fin.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    <span class="function">ifstream <span class="title">fin</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="built_in">string</span> s;
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            fin &gt;&gt; s;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    fin.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; fout.cpp:</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;ctime&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span>

<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>
</span>{
    <span class="keyword">int</span> testCnt = <span class="number">10</span>;
    <span class="keyword">int</span> caseCnt = <span class="number">10000000</span>;
    <span class="keyword">double</span> t_sum = <span class="number">0</span>;
    <span class="function">ofstream <span class="title">fout</span><span class="params">(<span class="string">"data"</span>)</span></span>;
    <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span> ; j &lt; testCnt ; j++)
    {
        <span class="keyword">long</span> t_start = clock();
        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; caseCnt ; i++)
        {
            fout &lt;&lt; <span class="string">"string"</span> &lt;&lt; i &lt;&lt; <span class="string">'\n'</span>;
        }
        <span class="keyword">long</span> t_end = clock();
        t_sum += (<span class="keyword">double</span>)(t_end - t_start) / CLOCKS_PER_SEC;
    }
    <span class="built_in">cout</span> &lt;&lt; <span class="string">"average cost: "</span> &lt;&lt; t_sum/testCnt &lt;&lt; <span class="string">"s\n"</span>;
    fout.close();
    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 下面是测试结果：</p>
<table><tr><th></th><th>scanf与printf</th><th>cin与cout</th><th>优化的cin与cout</th>    <th>fin与fout</th></tr><tr><td>输入</td><td>1.125694s</td><td>2.46949s</td><td>0.950055s</td><td>0.834976s</td></tr><tr><td>输出</td><td>1.304706s</td><td>1.46059s</td><td>1.41783s</td><td>1.41298s</td></tr></table>

<p>&nbsp;&nbsp;&nbsp; 可以看到，事实上，解开同步之前，cin耗时是scanf的两倍还多，cout则比printf略慢。解开枷锁的cin执行效率与直接读文件接近，甚至比C的scanf还要快。至于输出，解开同步的cout执行效率略有提高，同样接近直接写文件，但是确实还是比C的printf略慢一点。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 参加过ACM比赛和部分自己做过测评的人想必不难发现，C++的cin和cout虽然比起C的scanf和printf来方便很多，但是速度似乎会打很大折扣。于是不求甚解的人们一拍脑门，认定因为C更“底层”，封装程度更低，所以势必比C++的输入输出执行效率更高。然而如若C++有知，恐怕要苦笑了。因为C++正是为了兼容C的输入输出才要在cin、cout中进行指针同步，降低了执行效率。那么可能挽回这种效率损失吗？答案是可以。<br>]]>
    
    </summary>
    
      <category term="C++" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="cin" scheme="http://blog.bipedalbit.net/tags/cin/"/>
    
      <category term="cout" scheme="http://blog.bipedalbit.net/tags/cout/"/>
    
      <category term="printf" scheme="http://blog.bipedalbit.net/tags/printf/"/>
    
      <category term="scanf" scheme="http://blog.bipedalbit.net/tags/scanf/"/>
    
      <category term="C++" scheme="http://blog.bipedalbit.net/categories/C/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[nginx之回头补课]]></title>
    <link href="http://blog.bipedalbit.net/2015/9/30/nginx%E4%B9%8B%E5%9B%9E%E5%A4%B4%E8%A1%A5%E8%AF%BE/"/>
    <id>http://blog.bipedalbit.net/2015/9/30/nginx之回头补课/</id>
    <published>2015-09-30T04:50:34.000Z</published>
    <updated>2015-09-30T10:17:33.786Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 最近买了苗泽的那本《Nginx高性能Web服务器详解》，准备系统地学习下Nginx相关的知识。于是在二次科普的过程中发现之前对Nginx的一些理解和认识被刷新细化了。特此发文总结补课。<br><a id="more"></a></p>
<h1 id="一-_Web请求处理机制">一. Web请求处理机制</h1><h2 id="1-1_多进程方式">1.1 多进程方式</h2><p>&nbsp;&nbsp;&nbsp; 传统的多进程方式是指：每接到一个客户端请求，就由服务器主进程生成一个子进程来负责与客户端建立连接并进行交互。连接断开子进程即销毁。<br>&nbsp;&nbsp;&nbsp; 优点：<br>&nbsp;&nbsp;&nbsp; ①设计实现比较简单，许多细节被OS管理；<br>&nbsp;&nbsp;&nbsp; ②由于进程间独立，资源独立分配使用，运行时出错风险小，稳定性好；<br>&nbsp;&nbsp;&nbsp; ③OS的进程资源回收机制比较完备，垃圾不会积累。<br>&nbsp;&nbsp;&nbsp; 缺点：<br>&nbsp;&nbsp;&nbsp; 动态生成子进程涉及内存复制等操作，资源、时间开销比较大。发生大并发时对OS资源造成较大压力，将导致OS性能下降。<br>&nbsp;&nbsp;&nbsp; Apache最初的设计架构就采用了多进程方式，但是为了适应大并发的要求，对多进程方式进行了改进。<br>&nbsp;&nbsp;&nbsp; 多进程方式的短板在于动态生成进程时的开销，于是Apache采用了“预生成进程”的工作方式。顾名思义，在还没有接到客户端请求的空闲时间，预先生成若干工作进程。当接到客户端请求，主进程分配子进程与客户端进行交互。连接断开后子进程保留，做开销很小的适当清理后被主进程管理起来等待下一个客户端请求。“预生成进程”在一定程度上缓解了大并发时Web服务器对OS资源造成的压力。</p>
<h2 id="1-2_多线程方式">1.2 多线程方式</h2><p>&nbsp;&nbsp;&nbsp; 总体来说多线程方式与多进程方式十分类似，它们的区别在于，Web服务器接到客户端请求后，多线程方式下，主进程将动态派生一个工作线程来与客户端进行交互。<br>&nbsp;&nbsp;&nbsp; 同样是动态生成，OS产生一个线程的开销要远远小于产生一个进程的开销（在实验验证之前，我对此表示怀疑）。<br>&nbsp;&nbsp;&nbsp; 优点：<br>&nbsp;&nbsp;&nbsp; OS资源压力小于多进程方式。<br>&nbsp;&nbsp;&nbsp; 缺点：<br>&nbsp;&nbsp;&nbsp; OS对线程管理的支持甚少，线程调度规则、内存空间管理、线程资源管理都需要开发者自己考虑，增大了开发难度和出错风险。错误也容易在Web服务器持续运行的过程中发生堆积。<br>&nbsp;&nbsp;&nbsp; IIS服务器采用了多线程方式处理Web请求，它的稳定性相对不错。但是IIS Web服务器管理员仍然倾向于定期检查和重启服务器，以消除不可预知的错误堆积或其他隐患。</p>
<h1 id="二-_异步/同步、阻塞/非阻塞">二. 异步/同步、阻塞/非阻塞</h1><p>&nbsp;&nbsp;&nbsp; 异步与非阻塞，同步与阻塞的概念常被人混淆。事实上，异步/同步与阻塞/非阻塞发生在网络通信的不同阶段，前者发生在客户端与服务端的通信阶段，与请求处理过程无关；后者发生在请求处理阶段。</p>
<h2 id="2-1_异步/同步">2.1 异步/同步</h2><p>&nbsp;&nbsp;&nbsp; 在网络通信中同步机制指：发送方发送请求后，等到接收方发回响应后才发出下一个请求。发送端与接收端步调一致。异步机制：发送方是否发送请求不受接收方是否作出响应的影响。在接收方，发送方的请求形成队列，依次处理完成后依次进行响应。</p>
<h2 id="2-2_阻塞/非阻塞">2.2 阻塞/非阻塞</h2><p>&nbsp;&nbsp;&nbsp; 阻塞与非阻塞的对象，通常是网络套接字Socket，其实质是IO操作。阻塞方式中，IO结果返回前，当前线程从运行状态被挂起，直到IO结果返回，才进入就绪状态等待调度以继续运行；非阻塞方式中，线程不会被挂起，如果当前调用无法马上返回结果，一个失败消息将被<strong>立即返回</strong>，并继续执行下一个调用。</p>
<h2 id="2-3_同步阻塞方式">2.3 同步阻塞方式</h2><p>&nbsp;&nbsp;&nbsp; 发送方发送请求后一直等待响应；接收方处理请求时的IO操作如果不能马上返回结果则一直等待，返回结果后响应发送方。一个请求周期期间，发送方和接收方不能进行其他工作。比如，超市排队付账时，客户（发送方）向收款员（接收方）付款（发送请求）后需等待找零，期间不能做其他事；收款员要等待收款机返回结果（IO操作）后才能找零（响应请求），期间也只能等待。这种方式实现简单，效率不高。</p>
<h2 id="2-4_同步非阻塞方式">2.4 同步非阻塞方式</h2><p>&nbsp;&nbsp;&nbsp; 付款后顾客只能等着，收款员可以先干点给商品打包、玩手机之类的杂事。实际中不使用。</p>
<h2 id="2-5_异步阻塞方式">2.5 异步阻塞方式</h2><p>&nbsp;&nbsp;&nbsp; 付款后顾客可以干点跟后面顾客聊天、玩手机之类的杂事，收款员只能等着。实际中不使用。</p>
<h2 id="2-6_异步非阻塞方式">2.6 异步非阻塞方式</h2><p>&nbsp;&nbsp;&nbsp; 发送方发送请求后无需忙等，可以继续其他操作，有响应时再做出对应反应；接收方如果不能马上获得IO结果，将请求挂入请求队列，继续其他工作，返回IO结果时触发一个事件，接收方向发送方作出对应响应。比如，付款后顾客可以干点跟后面顾客聊天、玩手机之类的杂事；收款员完成商品扫码和收款金额输入后可以先干点给商品打包、玩手机之类的杂事，当收款机发出警报声，收款员告诉顾客机器坏了没法收款，当收款机刷新出找零金额，收款员开始找零。</p>
<h1 id="三-_Nginx的请求处理机制">三. Nginx的请求处理机制</h1><p>&nbsp;&nbsp;&nbsp; Nginx使用“预生成进程”或“带进程池”的多进程方式作为请求处理方式，使用异步非阻塞方式作为网络通信方式。Nginx服务器在启动时就由主进程fork（分支）若干固定数目的工作进程（工作进程数通常与CPU核心数线性相关，有助于提高CPU利用率），这些工作进程就是Nginx的进程池。使用多进程方式保证大并发情景下<strong>不增加</strong>OS资源压力，使用异步非阻塞方式保证大并发情景下<strong>不降低</strong>请求处理能力。</p>
<h1 id="四-_Nginx事件驱动模型">四. Nginx事件驱动模型</h1><h2 id="4-1_事件驱动模型概述">4.1 事件驱动模型概述</h2><p>&nbsp;&nbsp;&nbsp; 事件驱动模型一般由事件收集器、事件发送器和事件处理器三个基本单元组成：<br>&nbsp;&nbsp;&nbsp; ①事件收集器：事件收集器一般由一个事件循环程序构成，即无线循环监听各种预设类型的事件；<br>&nbsp;&nbsp;&nbsp; ②事件收集器：事件发送器实际上只是一个事件映射集；<br>&nbsp;&nbsp;&nbsp; ③时间处理器：通常，事件处理器的并发实现有以下三种方式：为请求创建新进程、为请求创建新线程、将请求放入请求队列。第一种方式OS资源开销大；第二种方式开发者需要自己动手完成线程同步，编码复杂；第三种方式编码逻辑复杂，但大多数Web服务器采用第三种方式，基于第三种方式的事件驱动模型逐渐形成了一系列“事件驱动处理库”，又被称为“多路IO复用方法”。</p>
<h2 id="4-2_select库">4.2 select库</h2><p>&nbsp;&nbsp;&nbsp; 各个版本的Linux和Windows平台都支持select库，接口基本相同，参数含义略有差异。首先它需要创建3个事件描述符集合（事件映射集），分别可以关注并收集Read、Write、Exception事件描述符（事件收集）。其次调用OS核心的select()函数。然后<strong>轮询</strong>每个事件描述符集中的每个事件描述符，如有事件发生则进行处理。Nginx服务器在编译过程中如未指定其他高性能事件驱动模型库，将自动编译 select 库。</p>
<h2 id="4-3_poll库">4.3 poll库</h2><p>&nbsp;&nbsp;&nbsp; 限于Linux平台的基本事件驱动模型。poll与select的区别在于，它只创建一个事件描述符集，而每个事件描述符<strong>结构上</strong>分别设置Read、Write、Exception事件，<strong>轮询</strong>时可同时检查三种事件是否发生。同样的，如未指定其他高性能事件驱动模型库，Nginx将自动编译 poll 库。</p>
<h2 id="4-4_epoll">4.4 epoll</h2><p>&nbsp;&nbsp;&nbsp; epoll是公认优秀的高性能事件驱动模型，Linux 2.6及以上版本可以使用它。epoll是对poll的改进，epoll可以看成是event poll，即基于事件的poll。epoll利用OS调用，让内核来创建事件描述符集，然后监听内核的反馈事件。当内核通过一个事件反馈发生事件的描述符列表再处理这些事件。<br>&nbsp;&nbsp;&nbsp; epoll库在Linux平台上是高效的（因为Linux内核的高效性），它支持一个进程打开大数目的事件描述符，上限是OS支持的最大打开文件数。同时因为它只处理内核上报的“活跃”描述符，epoll的IO效率不随描述符数目的增加线性下降（类似哈希查找之于遍历查找），即它支持大描述符集。</p>
<h2 id="4-5_rtsig模型">4.5 rtsig模型</h2><p>&nbsp;&nbsp;&nbsp; rtsig即real time signal的缩写，即实时信号。事实上，rtsig调用内核创建一个固定长度上限的事件信号发生队列，等待工作进程依次处理。</p>
<h2 id="4-6_其他事件驱动模型">4.6 其他事件驱动模型</h2><p>&nbsp;&nbsp;&nbsp; kqueue模型，是用于支持BSD系列平台（FreeBSD、OpenBSD、NetBSD、Mac OS X等）的高效事件驱动模型。也是poll的变种，仰仗OS内核支持条件触发（LT即level-triggered即满足条件触发）和边缘触发（ET即edge-triggered，即状态变换触发）。</p>
<p>&nbsp;&nbsp;&nbsp; /dev/poll模型，是用于支持Unix衍生平台（Solaris、HP/UX、IRIX、Tru64 UNIX），该模型是Sun公司开发Solaris系列平台时提出的事件驱动机制方案，使用了虚拟的/dev/poll设备，开发人员监听设备消息获取事件通知。</p>
<p>&nbsp;&nbsp;&nbsp; eventport模型，是用于支持Solaris10及以上版本平台的高效事件驱动模型。也是Sun公司开发Solaris是提出的方案，可以有效防止内核崩溃等情况。</p>
<h2 id="4-7_IOCP模型">4.7 IOCP模型</h2><p>&nbsp;&nbsp;&nbsp; 苗泽在书中没有提到，但现在Nginx在windows平台下还支持IOCP模型，虽然我对它不甚了解，但圈内提及Windows平台下的大并发网络，一定会提起IOCP模型，估计也是利用内核优化的事件驱动模型。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 最近买了苗泽的那本《Nginx高性能Web服务器详解》，准备系统地学习下Nginx相关的知识。于是在二次科普的过程中发现之前对Nginx的一些理解和认识被刷新细化了。特此发文总结补课。<br>]]>
    
    </summary>
    
      <category term="Web请求处理机制" scheme="http://blog.bipedalbit.net/tags/Web%E8%AF%B7%E6%B1%82%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/tags/nginx/"/>
    
      <category term="事件驱动模型" scheme="http://blog.bipedalbit.net/tags/%E4%BA%8B%E4%BB%B6%E9%A9%B1%E5%8A%A8%E6%A8%A1%E5%9E%8B/"/>
    
      <category term="同步/异步" scheme="http://blog.bipedalbit.net/tags/%E5%90%8C%E6%AD%A5-%E5%BC%82%E6%AD%A5/"/>
    
      <category term="阻塞/非阻塞" scheme="http://blog.bipedalbit.net/tags/%E9%98%BB%E5%A1%9E-%E9%9D%9E%E9%98%BB%E5%A1%9E/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/categories/nginx/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[socket.io邀请制带登录聊天室实例【DRRR-chat-room】]]></title>
    <link href="http://blog.bipedalbit.net/2015/9/12/socket-io%E9%82%80%E8%AF%B7%E5%88%B6%E5%B8%A6%E7%99%BB%E5%BD%95%E8%81%8A%E5%A4%A9%E5%AE%A4%E5%AE%9E%E4%BE%8B%E3%80%90DRRR-chat-room%E3%80%91/"/>
    <id>http://blog.bipedalbit.net/2015/9/12/socket-io邀请制带登录聊天室实例【DRRR-chat-room】/</id>
    <published>2015-09-12T14:44:25.000Z</published>
    <updated>2015-09-16T02:50:33.887Z</updated>
    <content type="html"><![CDATA[<p><br>    <strong>&nbsp;&nbsp;&nbsp; 写个博文其实也没什么特别要说的。研究node.js的过程中扩展了socket.io的聊天室demo，仿制了一个动漫DRRR（无头骑士异闻录）中的聊天室。使用邀请制，即注册需要提供邀请码。头一次在GitHub建项目：<a target="_blank" href="https://github.com/bipedalBit/DRRR-chat-room">DRRR-chat-room</a>。下个项目可能是用socket.io做一个真·实时弹幕站，尽请期待。</strong><br><br><br></p>
]]></content>
    <summary type="html">
    <![CDATA[<p><br>    <strong>&nbsp;&nbsp;&nbsp; 写个博文其实也没什么特别要说的。研究node.js的过程中扩展了socket.io的聊天室demo，仿制了一个动漫DRRR（无头骑士异闻录）中的聊天室。使用邀请制，即注册需要提供邀请码。头一次在GitHu]]>
    </summary>
    
      <category term="github" scheme="http://blog.bipedalbit.net/tags/github/"/>
    
      <category term="node.js" scheme="http://blog.bipedalbit.net/tags/node-js/"/>
    
      <category term="socket.io" scheme="http://blog.bipedalbit.net/tags/socket-io/"/>
    
      <category term="node.js" scheme="http://blog.bipedalbit.net/categories/node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[node.js的'Cannot enqueue Handshake after invoking quit'错误]]></title>
    <link href="http://blog.bipedalbit.net/2015/9/6/node.js%E7%9A%84'Cannot%20enqueue%20Handshake%20after%20invoking%20quit'%E9%94%99%E8%AF%AF/"/>
    <id>http://blog.bipedalbit.net/2015/9/6/node.js的'Cannot enqueue Handshake after invoking quit'错误/</id>
    <published>2015-09-06T10:11:25.000Z</published>
    <updated>2015-09-15T12:42:54.531Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 今天用node.js做无刷新聊天室的时候遇到”Error: Cannot enqueue Handshake after invoking quit.”的问题，在Stack Overflow被点化了。<br><a id="more"></a></p>
<pre><code>module.exports = {
    getDataFromUserGps: <span class="function"><span class="keyword">function</span><span class="params">(callback)</span>
    </span>{
        connection.connect();
        connection.query(<span class="string">"SELECT * FROM usergps"</span>,
            <span class="function"><span class="keyword">function</span><span class="params">(err, results, fields)</span> </span>{
                <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="keyword">callback</span>(err, <span class="literal">null</span>);
                <span class="keyword">return</span> <span class="keyword">callback</span>(<span class="literal">null</span>, results);
            }
        );
        connection.end();
    },
    loginUser: <span class="function"><span class="keyword">function</span><span class="params">(login, pass, callback)</span>
    </span>{
        connection.connect();
        connection.query(
            <span class="string">"SELECT id FROM users WHERE login = ? AND pass = ?"</span>,
            [login, pass],
            <span class="function"><span class="keyword">function</span><span class="params">(err, results, fields)</span>
            </span>{
                <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="keyword">callback</span>(err, <span class="literal">null</span>);
                <span class="keyword">return</span> <span class="keyword">callback</span>(<span class="literal">null</span>, results);
            }
        );
        connection.end();
    },
    getUserDetails: <span class="function"><span class="keyword">function</span><span class="params">(userid, callback)</span>
    </span>{
        connection.connect();
        connection.query(
            <span class="string">"SELECT * FROM userProfilDetails LEFT JOIN tags ON userProfilDetails.userId = tags.userId WHERE userProfilDetails.userid = ?"</span>,
            [userid],
            <span class="function"><span class="keyword">function</span><span class="params">(err, results, fields)</span>
            </span>{
                <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="keyword">callback</span>(err, <span class="literal">null</span>);
                <span class="keyword">return</span> <span class="keyword">callback</span>(<span class="literal">null</span>, results);
            }
        );
        connection.end();
    },
    addTags: <span class="function"><span class="keyword">function</span><span class="params">(userId, tags)</span>
    </span>{
        connection.connect();
        connection.query(
            <span class="string">"INSERT INTO tag (userId, tag) VALUES (?, ?)"</span>,
            [userId, tags],
            <span class="function"><span class="keyword">function</span><span class="params">(err, results, fields)</span>
            </span>{
                <span class="keyword">if</span> (err) <span class="keyword">throw</span> err;
            }
        )
        connection.end();
    }
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 一开始运行一切正常，但当我执行第二个&quot;query&quot;的时候，会报这个错：<br>&nbsp;&nbsp;&nbsp;<code>Cannot enqueue Handshake after invoking quit</code><br>&nbsp;&nbsp;&nbsp; 我试过一直不用.end()方法关掉连接，然并卵。<br>&nbsp;&nbsp;&nbsp; 先谢过。<br>&nbsp;&nbsp;&nbsp; Radex</p>
<p>&nbsp;&nbsp;&nbsp; <strong><span style="color:#FF0000;">那些瞎解答和水贴的我就不翻译了。</span></strong></p>
<p>&nbsp;&nbsp;&nbsp;根据:<br>&nbsp;&nbsp;&nbsp;Fixing Node Mysql &quot;Error: Cannot enqueue Handshake after invoking quit.&quot;:<br>&nbsp;&nbsp;&nbsp;<a href="http://codetheory.in/fixing-node-mysql-error-cannot-enqueue-handshake-after-invoking-quit/" target="_blank" rel="external">http://codetheory.in/fixing-node-mysql-error-cannot-enqueue-handshake-after-invoking-quit/</a><br>的<br>&nbsp;&nbsp;&nbsp; <strong>TL;DR</strong> <code>每次关掉连接之后你都需要用createConnection方法创建一个新连接。</code><br>以及<br><strong>&nbsp;&nbsp;&nbsp; Note:</strong> 如果你是在伺服web请求，你就不该在每次请求处理完时关掉连接了。服务器启动时创建一个连接，然后一直用connection/client对象查询就好。要处理服务器断开和重连事件时你可以监听错误事件。完整代码：<a target="_blank" href="https://github.com/felixge/node-mysql#server-disconnects">here</a>.<br>又根据:<br>&nbsp;&nbsp;&nbsp; Readme.md - Server disconnects:<br>&nbsp;&nbsp;&nbsp; <a href="https://github.com/felixge/node-mysql#server-disconnects" target="_blank" rel="external">https://github.com/felixge/node-mysql#server-disconnects</a><br>里边说</p>
<blockquote><br><h2>Server disconnects</h2><br>你可能因为网络问题、服务器超时或者服务器挂了而失去与MySQL服务器的连接。 所有这些情况都被视为&quot;fatal error&quot;，而且会有一个错误代码 <code>err.code = ‘PROTOCOL_CONNECTION_LOST’</code>。想了解更多可以看看错误处理章节。<br><br>处理这些不被期望的连接断开的最佳方式如下:<br></blockquote>

<pre><code><span class="function"><span class="keyword">function</span> <span class="title">handleDisconnect</span>(<span class="params">connection</span>) </span>{
    connection.on(<span class="string">'error'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>{
    <span class="keyword">if</span> (!err.fatal) {
      <span class="keyword">return</span>;
    }
    <span class="keyword">if</span> (err.code !== <span class="string">'PROTOCOL_CONNECTION_LOST'</span>) {
      <span class="keyword">throw</span> err;
    }
    <span class="built_in">console</span>.log(<span class="string">'Re-connecting lost connection: '</span> + err.stack);
    connection = mysql.createConnection(connection.config);
    handleDisconnect(connection);
    connection.connect();
  });
}

handleDisconnect(connection);
</code></pre><p><blockquote><br>&nbsp;&nbsp;&nbsp; 如上例所示，重连是通过创建一个新连接的方式达成的，因为连接对象被设计成一挂掉就无法重连。<br>&nbsp;&nbsp;&nbsp; 使用连接池的时候，挂掉的连接会被从连接池中移除并释放空间，新连接请求到来时自动创建一个新连接。<br></blockquote><br>&nbsp;&nbsp;&nbsp; 答题人后面贴了一段自己的自动重连代码，这里我就不贴了。</p>
<p>&nbsp;&nbsp;&nbsp; 回答于2013年5月3日18:58<br>&nbsp;&nbsp;&nbsp; XP1</p>
<p>&nbsp;&nbsp;&nbsp; 这个回答虽然没被题主采纳，但我和下面跟帖的一直认为这个回答比较好。<br><br>&nbsp;&nbsp;&nbsp; <a href="http://stackoverflow.com/questions/14087924/cannot-enqueue-handshake-after-invoking-quit" target="_blank" rel="external">原网页</a></p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 今天用node.js做无刷新聊天室的时候遇到”Error: Cannot enqueue Handshake after invoking quit.”的问题，在Stack Overflow被点化了。<br>]]>
    
    </summary>
    
      <category term="node.js" scheme="http://blog.bipedalbit.net/tags/node-js/"/>
    
      <category term="node.js" scheme="http://blog.bipedalbit.net/categories/node-js/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[【五】nginx的数据结构(3)——扩展红黑树]]></title>
    <link href="http://blog.bipedalbit.net/2015/8/18/%E3%80%90%E4%BA%94%E3%80%91nginx%E7%9A%84%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-3-%E2%80%94%E2%80%94%E6%89%A9%E5%B1%95%E7%BA%A2%E9%BB%91%E6%A0%91/"/>
    <id>http://blog.bipedalbit.net/2015/8/18/【五】nginx的数据结构-3-——扩展红黑树/</id>
    <published>2015-08-17T18:04:43.000Z</published>
    <updated>2015-09-16T15:39:04.376Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 基础版的红黑树写完之后总觉得有些辅助功能不补充一下就难以体现红黑树在统计方面的优越性，于是我又写了红黑树扩展版。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; 发扬我一贯的支线任务狂魔的作风，一晚上就完成了之前设想的红黑树扩展版本。<br>&nbsp;&nbsp;&nbsp; rbtree.h：</p>
<pre><code><span class="comment">/*
 * Copyright (C) Bipedal Bit
 * Verson 1.0.0.2
 */</span>

<span class="preprocessor">#<span class="keyword">ifndef</span> _RBTREE_H_INCLUDED_</span>
<span class="preprocessor">#<span class="keyword">define</span> _RBTREE_H_INCLUDED_</span>

<span class="comment">/* the node structure of the red-black tree */</span>
<span class="keyword">typedef</span> <span class="keyword">struct</span> rbtree_node_s <span class="keyword">rbtree_node_t</span>;
<span class="comment">/* Using type int means its range is -0x7fffffff-1~0x7fffffff. */</span>
<span class="keyword">typedef</span> <span class="keyword">int</span> <span class="keyword">rbtree_key_t</span>;
<span class="comment">/* Abstract type is complicated to achieve with C so I use char* instead. */</span>
<span class="keyword">typedef</span> <span class="keyword">char</span>* <span class="keyword">rbtree_data_t</span>;

<span class="keyword">struct</span> rbtree_node_s
{
    <span class="comment">/* key of the node */</span>
    <span class="keyword">rbtree_key_t</span>    key;
    <span class="comment">/* pointer of the parent of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    parent;
    <span class="comment">/* pointer of the left kid of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    left;
    <span class="comment">/* pointer of the right kid of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    right;
    <span class="comment">/* color of the node */</span>
    <span class="keyword">unsigned</span> <span class="keyword">char</span>    color;
    <span class="comment">/* pointer of the value of the node corresponding to the key */</span>
    <span class="keyword">rbtree_data_t</span>    value;
    <span class="comment">/* count of nodes in the subtree whose root is the current node */</span>
    <span class="keyword">int</span> node_cnt;
};

<span class="comment">/* the tree object stucture of the red-black tree */</span>
<span class="keyword">typedef</span> <span class="keyword">struct</span> rbtree_s <span class="keyword">rbtree_t</span>;
<span class="comment">/* foundational insert function pointer */</span>
<span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*rbtree_insert_p)</span> <span class="params">(rbtree_t* root, rbtree_node_t* node)</span></span>;
<span class="comment">/* foundational visit function pointer */</span>
<span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*rbtree_visit_p)</span> <span class="params">(rbtree_node_t* node)</span></span>;

<span class="keyword">struct</span> rbtree_s
{
    <span class="comment">/* the pointer of the root node of the tree */</span>
    <span class="keyword">rbtree_node_t</span>* root;
    <span class="comment">/* black leaf nodes as sentinel */</span>
    <span class="keyword">rbtree_node_t</span>* sentinel;
    <span class="comment">/* the polymorphic insert function pointer */</span>
    rbtree_insert_p insert;
};

<span class="comment">/* macros */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_init(tree, s, i)        \
rbtree_sentinel_init(s);            \
(tree)-&gt;root = s;                \
(tree)-&gt;sentinel = s;            \
(tree)-&gt;insert = i</span>

<span class="preprocessor">#<span class="keyword">define</span> rbtree_red(node)    ((node)-&gt;color = <span class="number">1</span>)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_black(node)    ((node)-&gt;color = <span class="number">0</span>)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_is_red(node)    ((node)-&gt;color)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_is_black(node)    (!rbtree_is_red(node))</span>
 <span class="comment">/* copy n2's color to n1 */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_copy_color(n1, n2)    (n1-&gt;color = n2-&gt;color)</span>
<span class="comment">/* sentinel must be black cuz it's leaf node */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_sentinel_init(node)    \
rbtree_black(node);            \
(node)-&gt;node_cnt = <span class="number">0</span></span>

<span class="comment">/* statements of public methods */</span>
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_insert_value</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_insert</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_delete</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="comment">/* get node by key */</span>
<span class="keyword">rbtree_node_t</span>* rbtree_find(<span class="keyword">rbtree_t</span>* tree, <span class="keyword">rbtree_key_t</span> key);
<span class="comment">/* get node by order number */</span>
<span class="keyword">rbtree_node_t</span>* rbtree_index(<span class="keyword">rbtree_t</span>* tree, <span class="keyword">int</span> index);
<span class="function"><span class="keyword">int</span> <span class="title">rbtree_height</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">int</span> <span class="title">rbtree_count</span><span class="params">(rbtree_t* tree)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_visit</span><span class="params">(rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_traversal</span><span class="params">(rbtree_t* tree, rbtree_node_t* node, rbtree_visit_p)</span></span>;

<span class="preprocessor">#<span class="keyword">endif</span>    <span class="comment">/* _RBTREE_H_INCLUDED_ */</span></span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 可以看到，我增加了按序号查找结点、求树高、求结点数、可重写访问节点方法的遍历，这么几个功能。<br>&nbsp;&nbsp;&nbsp; 为了提高按序号查找结点的效率，我增加了一个结点项node_cnt，代表当前结点为根的子树上的结点总数。这样按序号查找结点的过程将是一个二分查找，时间效率与按key查找相同，都是O(log2n)。<br>&nbsp;&nbsp;&nbsp; 遍历方法使用递归的中序遍历，默认的结点访问方法是个空方法，用户可以自行重写。<br>&nbsp;&nbsp;&nbsp; rbtree.c：</p>
<pre><code><span class="comment">/*
 * Copyright (C) Bipedal Bit
 * Verson 1.0.0.2
 */</span>

<span class="variable">#include</span> &lt;stddef<span class="built_in">.</span>h&gt;
<span class="variable">#include</span> <span class="string">"rbtree.h"</span>

<span class="comment">/* inline methods */</span>
<span class="comment">/* get the node with the minimum key in a subtree of the red-black tree */</span>
static <span class="keyword">inline</span> rbtree_node_t*
rbtree_subtree_min(rbtree_node_t* node, rbtree_node_t* sentinel)
{
    <span class="keyword">while</span>(node<span class="subst">-&gt;</span>left != sentinel)
    {
        node = node<span class="subst">-&gt;</span>left;
    }

    <span class="keyword">return</span> node;
}

<span class="comment">/* replace the node "node" in the tree with node "tmp" */</span>
static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_replace(rbtree_t* tree,
    rbtree_node_t* node, rbtree_node_t* tmp)
{
    <span class="comment">/* upward: p[node] &lt;- p[tmp] */</span>
    tmp<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;

    <span class="keyword">if</span> (node == tree<span class="subst">-&gt;</span>root)
    {
        tree<span class="subst">-&gt;</span>root = tmp;
    }
    <span class="keyword">else</span> <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
    {
        <span class="comment">/* downward: left[p[node]] &lt;- tmp */</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = tmp;
    }
    <span class="keyword">else</span>
    {
        <span class="comment">/* downward: right[p[node]] &lt;- tmp */</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = tmp;
    }

    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = tmp;
}

<span class="comment">/* change the topologic structure of the tree keeping the order of the nodes */</span>
static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_left_rotate(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="comment">/* node as the var x in CLRS while tmp as the var y */</span>
    rbtree_node_t* tmp = node<span class="subst">-&gt;</span>right;

    <span class="comment">/* fix node_cnt */</span>
    node<span class="subst">-&gt;</span>node_cnt = node<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span>node_cnt + tmp<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span>node_cnt + <span class="number">1</span>;
    tmp<span class="subst">-&gt;</span>node_cnt = node<span class="subst">-&gt;</span>node_cnt + tmp<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span>node_cnt + <span class="number">1</span>;

    <span class="comment">/* replace y with left[y] */</span>
    <span class="comment">/* downward: right[x] &lt;- left[y] */</span>
    node<span class="subst">-&gt;</span>right = tmp<span class="subst">-&gt;</span>left;
    <span class="comment">/* if left[[y] is not NIL it has a parent */</span>
    <span class="keyword">if</span> (tmp<span class="subst">-&gt;</span>left != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="comment">/* upward: p[left[y]] &lt;- x */</span>
        tmp<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span><span class="keyword">parent</span> = node;
    }

    <span class="comment">/* replace x with y */</span>
    rbtree_replace(tree, node, tmp);
    tmp<span class="subst">-&gt;</span>left = node;
}

static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_right_rotate(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* tmp = node<span class="subst">-&gt;</span>left;

    <span class="comment">/* fix node_cnt */</span>
    node<span class="subst">-&gt;</span>node_cnt = node<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span>node_cnt + tmp<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span>node_cnt + <span class="number">1</span>;
    tmp<span class="subst">-&gt;</span>node_cnt = node<span class="subst">-&gt;</span>node_cnt + tmp<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span>node_cnt + <span class="number">1</span>;

    <span class="comment">/* replace y with right[y] */</span>
    node<span class="subst">-&gt;</span>left = tmp<span class="subst">-&gt;</span>right;
    <span class="keyword">if</span> (tmp<span class="subst">-&gt;</span>right != tree<span class="subst">-&gt;</span>sentinel)
    {
        tmp<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span><span class="keyword">parent</span> = node;
    }

    <span class="comment">/* replace x with y */</span>
    rbtree_replace(tree, node, tmp);
    tmp<span class="subst">-&gt;</span>right = node;
}

<span class="comment">/* static methods */</span>
<span class="comment">/* fix the red-black tree after the new node inserted */</span>
static <span class="literal">void</span> rbtree_insert_fixup(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="keyword">while</span>(rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>))
    {
        <span class="keyword">if</span> (node<span class="subst">-&gt;</span><span class="keyword">parent</span> == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            <span class="comment">/* case 1: node's uncle is red */</span>
            <span class="keyword">if</span> (rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right))
            {
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;
                <span class="comment">/* Then we can consider the whole subtree */</span>
                <span class="comment">/* which is represented by the new "node" as the "node" before */</span>
                <span class="comment">/* and keep looping till "node" become the root. */</span>
            }
            <span class="comment">/* case 2: node's uncle is black */</span>
            <span class="keyword">else</span>
            {
                <span class="comment">/* ensure node is the left kid of its parent */</span>
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right)
                {
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
                    rbtree_left_rotate(tree, node);
                }
                <span class="comment">/* case 2 -&gt; case 1 */</span>
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_right_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
            }
        }
        <span class="comment">/* same as the "if" clause before with "left" and "right" exchanged */</span>
        <span class="keyword">else</span>
        {
            <span class="keyword">if</span> (rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left))
            {
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
                {
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
                    rbtree_right_rotate(tree, node);
                }
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
            }
        }
    }
    <span class="comment">/* ensure the root node being black */</span>
    rbtree_black(tree<span class="subst">-&gt;</span>root);
}

static <span class="literal">void</span> rbtree_delete_fixup(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* brother = <span class="built_in">NULL</span>;

    <span class="keyword">while</span>(node != tree<span class="subst">-&gt;</span>root <span class="subst">&amp;&amp;</span> rbtree_is_black(node))
    {
        <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
            <span class="keyword">if</span> (rbtree_is_red(brother))
            {
                rbtree_black(brother);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                <span class="comment">/* update brother after topologic change of the tree */</span>
                brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
            }

            <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> rbtree_is_black(brother<span class="subst">-&gt;</span>right))
            {
                rbtree_red(brother);
                <span class="comment">/* go upward and keep on fixing color */</span>
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>right))
                {
                    rbtree_black(brother<span class="subst">-&gt;</span>left);
                    rbtree_red(brother);
                    rbtree_right_rotate(tree, brother);
                    <span class="comment">/* update brother after topologic change of the tree */</span>
                    brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
                }
                rbtree_copy_col<span class="subst">or</span>(brother, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(brother<span class="subst">-&gt;</span>right);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                <span class="comment">/* end the loop and ensure root is black */</span>
                node = tree<span class="subst">-&gt;</span>root;
            }
        }
        <span class="comment">/* same as the "if" clause before with "left" and "right" exchanged */</span>
        <span class="keyword">else</span>
        {
            brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            <span class="keyword">if</span> (rbtree_is_red(brother))
            {
                rbtree_black(brother);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            }

            <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> rbtree_is_black(brother<span class="subst">-&gt;</span>right))
            {
                rbtree_red(brother);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left))
                {
                    rbtree_black(brother<span class="subst">-&gt;</span>right);
                    rbtree_red(brother);
                    rbtree_right_rotate(tree, brother);
                    brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
                }
                rbtree_copy_col<span class="subst">or</span>(brother, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(brother<span class="subst">-&gt;</span>left);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = tree<span class="subst">-&gt;</span>root;
            }
        }
    }

    rbtree_black(node);
}

<span class="comment">/* public methods */</span>
<span class="literal">void</span> rbtree_insert_value(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="comment">/* Using ** to know wether the new node will be a left kid */</span>
    <span class="comment">/* or a right kid of its parent node. */</span>
    rbtree_node_t** tmp = <span class="subst">&amp;</span>tree<span class="subst">-&gt;</span>root;
    rbtree_node_t* <span class="keyword">parent</span>;

    <span class="keyword">while</span>(*tmp != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="keyword">parent</span> = *tmp;

        <span class="comment">/* update node_cnt */</span>
        (<span class="keyword">parent</span><span class="subst">-&gt;</span>node_cnt)++;

        tmp = (node<span class="subst">-&gt;</span>key &lt; <span class="keyword">parent</span><span class="subst">-&gt;</span>key) ? <span class="subst">&amp;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left : <span class="subst">&amp;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
    }

    <span class="comment">/* The pointer knows wether the node should be on the left side */</span>
    <span class="comment">/* or on the right one. */</span>
    *tmp = node;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="keyword">parent</span>;
    node<span class="subst">-&gt;</span>left = tree<span class="subst">-&gt;</span>sentinel;
    node<span class="subst">-&gt;</span>right = tree<span class="subst">-&gt;</span>sentinel;
    rbtree_red(node);
}

<span class="literal">void</span> rbtree_visit(rbtree_node_t* node)
{
    <span class="comment">/* visiting the current node */</span>
}

<span class="literal">void</span> rbtree_insert(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* sentinel = tree<span class="subst">-&gt;</span>sentinel;

    <span class="comment">/* if the tree is empty */</span>
    <span class="keyword">if</span> (tree<span class="subst">-&gt;</span>root == sentinel)
    {
        tree<span class="subst">-&gt;</span>root = node;
        node<span class="subst">-&gt;</span><span class="keyword">parent</span> = sentinel;
        node<span class="subst">-&gt;</span>left = sentinel;
        node<span class="subst">-&gt;</span>right = sentinel;
        rbtree_black(node);

        <span class="keyword">return</span>;
    }

    <span class="comment">/* generally */</span>
    tree<span class="subst">-&gt;</span>insert(tree, node);
    rbtree_insert_fixup(tree, node);
}

<span class="literal">void</span> rbtree_delete(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* sentinel = tree<span class="subst">-&gt;</span>sentinel;
    <span class="comment">/* wether "node" is on the left side or the right one */</span>
    rbtree_node_t** ptr_to_node = <span class="built_in">NULL</span>;
    <span class="comment">/* "cover" is the node which is going to cover "node" */</span>
    rbtree_node_t* cover = <span class="built_in">NULL</span>;
    <span class="comment">/* wether we lossing a red node on the edge of the tree */</span>
    int loss_red = rbtree_is_red(node);
    int is_root = (node == tree<span class="subst">-&gt;</span>root);

    <span class="comment">/* get "cover" &amp; "loss_red"  */</span>
    <span class="comment">/* sentinel in "node"'s kids */</span>
    <span class="keyword">if</span> (node<span class="subst">-&gt;</span>left == sentinel)
    {
        cover = node<span class="subst">-&gt;</span>right;
    }
    <span class="keyword">else</span> <span class="keyword">if</span> (node<span class="subst">-&gt;</span>right == sentinel)
    {
        cover = node<span class="subst">-&gt;</span>left;
    }
    <span class="comment">/* "node"'s kids are both non-sentinel */</span>
    <span class="keyword">else</span>
    {
        <span class="comment">/* update "node" &amp; "loss_red" &amp; "is_root" &amp; "cover" */</span>
        cover = rbtree_subtree_min(node<span class="subst">-&gt;</span>right, sentinel);
        node<span class="subst">-&gt;</span>key = cover<span class="subst">-&gt;</span>key;
        node<span class="subst">-&gt;</span>value = cover<span class="subst">-&gt;</span>value;
        node = cover;
        loss_red = rbtree_is_red(node);
        is_root = <span class="number">0</span>;
        <span class="comment">/* move "cover"'s kids */</span>
        <span class="comment">/* "cover" can only be a left kid */</span>
        <span class="comment">/* and can only have a right non-sentinel kid */</span>
        <span class="comment">/* because of function "rbtree_subtree_min" */</span>
        cover = node<span class="subst">-&gt;</span>right;
    }

    <span class="keyword">if</span> (is_root)
    {
        <span class="comment">/* update root */</span>
        tree<span class="subst">-&gt;</span>root = cover;
    }
    <span class="keyword">else</span>
    {
        <span class="comment">/* downward link */</span>
        <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = cover;
        }
        <span class="keyword">else</span>
        {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = cover;
        }
    }
    <span class="comment">/* upward link */</span>
    cover<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
    <span class="comment">/* "cover" may be a sentinel */</span>
    <span class="keyword">if</span> (cover != sentinel)
    {
        <span class="comment">/* set "cover" */</span>
        cover<span class="subst">-&gt;</span>left = node<span class="subst">-&gt;</span>left;
        cover<span class="subst">-&gt;</span>right = node<span class="subst">-&gt;</span>right;
        rbtree_copy_col<span class="subst">or</span>(cover, node);
    }

    <span class="comment">/* clear "node" since it's useless */</span>
    node<span class="subst">-&gt;</span>key = -<span class="number">1</span>;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>left = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>right = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>value = <span class="built_in">NULL</span>;

    <span class="comment">/* update node_cnt */</span>
    rbtree_node_t* tmp = cover<span class="subst">-&gt;</span><span class="keyword">parent</span>;
    <span class="keyword">while</span>(tmp != sentinel)
    {
        (tmp<span class="subst">-&gt;</span>node_cnt)--;
        tmp = tmp<span class="subst">-&gt;</span><span class="keyword">parent</span>;
    }

    <span class="keyword">if</span> (loss_red)
    {
        <span class="keyword">return</span>;
    }

    <span class="comment">/* When lossing a black node on edge */</span>
    <span class="comment">/* the fifth rule of red-black tree will be broke. */</span>
    <span class="comment">/* So the tree need to be fixed. */</span>
    rbtree_delete_fixup(tree, cover);
}

<span class="comment">/* find the node in the tree corresponding to the given key value */</span>
rbtree_node_t* rbtree_find(rbtree_t* tree, rbtree_key_t key)
{
    rbtree_node_t* tmp = tree<span class="subst">-&gt;</span>root;
    <span class="comment">/* next line is just fot test */</span>
    <span class="comment">// int step_cnt = 0;</span>

    <span class="comment">/* search the binary tree */</span>
    <span class="keyword">while</span>(tmp != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="comment">/* next line is just fot test */</span>
        <span class="comment">// step_cnt++;</span>
        <span class="keyword">if</span>(key == tmp<span class="subst">-&gt;</span>key)
        {
            <span class="comment">/* next line is just for test */</span>
            <span class="comment">// printf("step count: %d, color: %s, ", step_cnt, rbtree_is_red(tmp) ? "red" : "black");</span>
            <span class="keyword">return</span> tmp;
        }

        tmp = (key &lt; tmp<span class="subst">-&gt;</span>key) ? tmp<span class="subst">-&gt;</span>left : tmp<span class="subst">-&gt;</span>right;
    }

    <span class="keyword">return</span> <span class="built_in">NULL</span>;
}

<span class="comment">/* find the node in the tree corresponding to the given order number */</span>
rbtree_node_t* rbtree_index(rbtree_t* tree, int index)
{
    <span class="keyword">if</span> (index &lt; <span class="number">0</span> <span class="subst">||</span> index &gt;= rbtree_count(tree))
    {
        <span class="keyword">return</span> <span class="built_in">NULL</span>;
    }

    rbtree_node_t* tmp = tree<span class="subst">-&gt;</span>root;
    int left_cnt = <span class="number">0</span>;
    int sub_left_cnt;

    <span class="keyword">while</span>(tmp<span class="subst">-&gt;</span>node_cnt &gt; <span class="number">0</span>)
    {
        sub_left_cnt = tmp<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span>node_cnt;
        <span class="keyword">if</span> (left_cnt + sub_left_cnt == index)
        {
            <span class="keyword">return</span> tmp;
        }

        <span class="keyword">if</span> (left_cnt + sub_left_cnt &lt; index)
        {
            left_cnt += sub_left_cnt + <span class="number">1</span>;
            tmp = tmp<span class="subst">-&gt;</span>right;
        }
        <span class="keyword">else</span>
        {
            tmp = tmp<span class="subst">-&gt;</span>left;
        }
    }
}

<span class="comment">/* get the height of the subtree */</span>
int rbtree_height(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="keyword">if</span> (node == tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="keyword">return</span> <span class="number">0</span>;
    }

    int left_height = rbtree_height(tree, node<span class="subst">-&gt;</span>left);
    int right_height = rbtree_height(tree, node<span class="subst">-&gt;</span>right);
    int sub_height = (left_height &gt; right_height) ? left_height : right_height;
    <span class="keyword">return</span> sub_height+<span class="number">1</span>;
}

<span class="comment">/* get the count of nodes in the tree */</span>
int rbtree_count(rbtree_t* tree)
{
    <span class="keyword">return</span> tree<span class="subst">-&gt;</span>root<span class="subst">-&gt;</span>node_cnt;
}

<span class="comment">/* visit every node of the subtree whose root is given in order */</span>
<span class="literal">void</span> rbtree_traversal(rbtree_t* tree, rbtree_node_t* node, rbtree_visit_p visit)
{
    <span class="keyword">if</span> (node != tree<span class="subst">-&gt;</span>sentinel)
    {
        rbtree_traversal(tree, node<span class="subst">-&gt;</span>left, visit);
        visit(node);
        rbtree_traversal(tree, node<span class="subst">-&gt;</span>right, visit);
    }
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 还是做个压力测试。<br>&nbsp;&nbsp;&nbsp; test.c：</p>
<pre><code><span class="preprocessor">#include <span class="title">&lt;stdio.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">&lt;stdlib.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">&lt;time.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">"rbtree.h"</span></span>

<span class="keyword">int</span> main(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])
{
    <span class="keyword">double</span> duration;
    <span class="keyword">double</span> room;

    rbtree_t t = {};
    rbtree_node_t s = {};
    rbtree_init(&amp;t, &amp;s, rbtree_insert_value);

    <span class="keyword">const</span> <span class="keyword">int</span> cnt = <span class="number">1</span>&lt;&lt;<span class="number">20</span>;
    <span class="keyword">const</span> <span class="keyword">int</span> max_len = <span class="number">15</span>;

<span class="preprocessor">#define TEST_VALUES {<span class="title">"apple"</span>, <span class="title">"banana"</span>, <span class="title">"cherry"</span>, <span class="title">"grape"</span>, <span class="title">"lemon"</span>, <span class="title">"mango"</span>, <span class="title">"pear"</span>, <span class="title">"pineapple"</span>, <span class="title">"strawberry"</span>, <span class="title">"watermelon"</span>}</span>

    <span class="comment">/* for gcc */</span>
    <span class="keyword">char</span>* v[] = TEST_VALUES;
    <span class="comment">/* for g++ */</span>
    <span class="comment">// char v[][max_len] = TEST_VALUES;</span>

    <span class="comment">/* Default stack size in Ubuntu Kylin 14.04 is 8MB. */</span>
    <span class="comment">/* It's not enough. So I use memory in heap which offers a lot larger room. */</span>
    rbtree_node_t* n = (rbtree_node_t*)calloc(cnt, <span class="keyword">sizeof</span>(rbtree_node_t));
    <span class="keyword">int</span> i;

    <span class="keyword">long</span> time1 = clock();

    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; cnt; i++)
    {
        n[i]<span class="variable">.key</span> = i+<span class="number">1</span>;
        n[i]<span class="variable">.value</span> = v[i%<span class="number">10</span>];
        n[i]<span class="variable">.node_cnt</span> = <span class="number">1</span>;
        rbtree_insert(&amp;t, &amp;n[i]);
    }

    srand( (<span class="keyword">unsigned</span> <span class="keyword">int</span>)time(<span class="number">0</span>) );
    <span class="keyword">int</span> no = rand()%cnt;
    printf(<span class="string">"n[%d]-&gt;key = %d\n"</span>, no, rbtree_index(&amp;t, no)-&gt;key);

    <span class="keyword">long</span> time2 = clock();
    room = <span class="number">48.0</span>*cnt/(<span class="number">1</span>&lt;&lt;<span class="number">20</span>);
    duration = (<span class="keyword">double</span>)(time2 - time1) / CLOCKS_PER_SEC;
    printf(<span class="string">"Inserting %d nodes costs %.2fMB and spends %f seconds.\n"</span>, cnt, room, duration);

    <span class="keyword">const</span> <span class="keyword">int</span> search_cnt = <span class="number">1</span>&lt;&lt;<span class="number">10</span>;
    <span class="keyword">for</span>( i = <span class="number">0</span> ; i &lt; search_cnt ; i++ )
    {
        rbtree_find(&amp;t, (rand()%cnt)+<span class="number">1</span>);
    }

    <span class="keyword">long</span> time3 = clock();
    duration = (<span class="keyword">double</span>)(time3 - time2) / CLOCKS_PER_SEC;
    printf(<span class="string">"Searching %d nodes among %d spends %f seconds.\n"</span>, search_cnt, cnt, duration);

    <span class="keyword">const</span> <span class="keyword">int</span> index_cnt = <span class="number">1</span>&lt;&lt;<span class="number">10</span>;
    <span class="keyword">for</span>( i = <span class="number">0</span> ; i &lt; index_cnt ; i++ )
    {
        rbtree_index(&amp;t, (rand()%cnt));
    }

    <span class="keyword">long</span> time4 = clock();
    duration = (<span class="keyword">double</span>)(time4 - time3) / CLOCKS_PER_SEC;
    printf(<span class="string">"Indexing %d nodes among %d spends %f seconds.\n"</span>, index_cnt, cnt, duration);

    <span class="keyword">const</span> <span class="keyword">int</span> delete_cnt = <span class="number">1</span>&lt;&lt;<span class="number">10</span>;
    <span class="keyword">int</span> nums[delete_cnt];
    <span class="keyword">int</span> num;
    <span class="comment">/* Let's hash! */</span>
    <span class="keyword">char</span>* mark = (<span class="keyword">char</span>*)calloc(cnt, <span class="keyword">sizeof</span>(<span class="keyword">char</span>));
    memset(mark, <span class="number">0</span>, cnt*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));
    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; delete_cnt; i++)
    {
        <span class="keyword">for</span>(;;)
        {
            num = rand()%cnt;
            <span class="keyword">if</span> (mark[num] == <span class="number">0</span>)
            {
                mark[num] = <span class="number">1</span>;
                nums[i] = num;
                <span class="keyword">break</span>;
            }
        }
    }

    <span class="keyword">long</span> time5 = clock();
    duration = (<span class="keyword">double</span>)(time5 - time4) / CLOCKS_PER_SEC;
    printf(<span class="string">"Hash %d times spends %f seconds.\n"</span>, delete_cnt, duration);

    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; delete_cnt; i++)
    {
        rbtree_delete(&amp;t, &amp;n[nums[i]]);
    }

    <span class="keyword">long</span> time6 = clock();
    duration = (<span class="keyword">double</span>)(time6 - time5) / CLOCKS_PER_SEC;
    printf(<span class="string">"Deleting %d nodes among %d spends %f seconds.\n"</span>, delete_cnt, cnt, duration);
    free(mark);

    <span class="keyword">int</span> h = rbtree_height(&amp;t, t<span class="variable">.root</span>);
    <span class="keyword">long</span> time7 = clock();
    duration = (<span class="keyword">double</span>)(time7 - time6) / CLOCKS_PER_SEC;
    printf(<span class="string">"The height of the tree is %d. Getting it spends %f seconds.\n"</span>, h, duration);

    rbtree_traversal(&amp;t, t<span class="variable">.root</span>, rbtree_visit);
    <span class="keyword">long</span> time8 = clock();
    duration = (<span class="keyword">double</span>)(time8 - time7) / CLOCKS_PER_SEC;
    printf(<span class="string">"Traversal the tree spends %f seconds.\n"</span>, duration);

    printf(<span class="string">"Count of nodes in the tree is %d.\n"</span>, rbtree_count(&amp;t));

    free(n);

    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 上一个版本的压力测试结果：</p>
<pre><code>Inserting <span class="number">1048576</span> nodes costs <span class="number">48.00</span>MB and spends <span class="number">0.425416</span> seconds.
Searching <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.001140</span> seconds.
Hash <span class="number">1024</span> times spends <span class="number">0.000334</span> seconds.
Deleting <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.000783</span> seconds.
</code></pre><p>&nbsp;&nbsp;&nbsp; 扩展版本的压力测试结果：</p>
<pre><code>Inserting <span class="number">1048576</span> nodes costs <span class="number">48.00</span>MB and spends <span class="number">0.467859</span> seconds.
Searching <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.001188</span> seconds.
Indexing <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.001484</span> seconds.
Hash <span class="number">1024</span> times spends <span class="number">0.000355</span> seconds.
Deleting <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.001417</span> seconds.
The height of the tree is <span class="number">28.</span> Getting it spends <span class="number">0.021669</span> seconds.
Traversal the tree spends <span class="number">0.023913</span> seconds.
Count of nodes in the tree is <span class="number">1047552.</span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 比较一下可以发现：<br>&nbsp;&nbsp;&nbsp; 1.插入结点略慢了一点，因为插入时多维护了一个node_cnt项。<br>&nbsp;&nbsp;&nbsp; 2.按key查找结点速度没有变化。<br>&nbsp;&nbsp;&nbsp; 3.哈希查找速度没有变化。<br>&nbsp;&nbsp;&nbsp; 4.删除结点花的时间几乎是原来的两倍，因为每次删除后都要一路向上更新node_cnt，几乎相当于包含了一次按key查询。<br>&nbsp;&nbsp;&nbsp; 5.按序号查询比按key查询略慢，因为每次进入右子树需要多做一次加法。<br>&nbsp;&nbsp;&nbsp; 6.遍历花的时间与求树高相同，因为它们的实质都是遍历树，时间效率O(n)数量级，具体点为2n次结点访问，分别为结点入栈和出栈时。<br>&nbsp;&nbsp;&nbsp; 别问我max、min、mid在哪，能按序号查询了这些还是问题吗？</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 基础版的红黑树写完之后总觉得有些辅助功能不补充一下就难以体现红黑树在统计方面的优越性，于是我又写了红黑树扩展版。<br>]]>
    
    </summary>
    
      <category term="C" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/tags/nginx/"/>
    
      <category term="rbtree" scheme="http://blog.bipedalbit.net/tags/rbtree/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/categories/nginx/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Linux下Sublime的C/C++多文件程序Makefile+清理+执行配置]]></title>
    <link href="http://blog.bipedalbit.net/2015/8/16/Linux%E4%B8%8BSublime%E7%9A%84C-C++%E5%A4%9A%E6%96%87%E4%BB%B6%E7%A8%8B%E5%BA%8FMakefile-%E6%B8%85%E7%90%86-%E6%89%A7%E8%A1%8C%E9%85%8D%E7%BD%AE/"/>
    <id>http://blog.bipedalbit.net/2015/8/16/Linux下Sublime的C-C++多文件程序Makefile-清理-执行配置/</id>
    <published>2015-08-16T14:45:46.000Z</published>
    <updated>2015-09-15T12:51:55.579Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 不自己动手为Sublime配个C/C++的通用Makefile脚本吗？不能自动编译链接多文件的编辑器不是好IDE。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; 首先我们需要了解几个事实：</p>
<p>&nbsp;&nbsp;&nbsp; 1.Sublime自带的C/C++编译+执行配置使用的是g++编译器，此外这套配置只针对单文件程序。如果要编译多文件，只能采用入口函数所在文件包含需要的源文件而非头文件的方式。比起我们熟悉的几种IDE，这显然无法令人接受。</p>
<p>&nbsp;&nbsp;&nbsp; 2.Linux下的C/C++程序员应该熟悉Makefile，Makefile可以算是一种脚本，执行关键字是一般是make，它可以设计多文件程序的编译+链接过程，生成.o文件和可执行文件。</p>
<p>&nbsp;&nbsp;&nbsp; 3.Sublime也有默认的Make配置，但是第一，Sublime没有提供通用的C/C++的Makefile脚本（自动组织而非每次手动重写），第二点是个小瑕疵，Sublime提供的Make配置只有编译、链接、清理功能，执行程序需要手动。</p>
<p>&nbsp;&nbsp;&nbsp; 之前重写红黑树最重要的副产品就是一个通用的C/C++的Makefile脚本。每次使用基本只需要改改输出的可执行文件名，或者干脆想个默认文件名以后都不改了，gcc/g++编译器的选择是自动的，当然也可以手动配置一些编译选项，这个自己看脚本注释就好。上干货：</p>
<pre><code><span class="comment">#############################################################</span>
<span class="comment"># Generic Makefile for C/C++ Program</span>
<span class="comment">#</span>
<span class="comment"># License: GPL (General Public License)</span>
<span class="comment"># Author:  whyglinux &lt;whyglinux AT gmail DOT com&gt;</span>
<span class="comment"># Date:    2006/03/04 (version 0.1)</span>
<span class="comment">#          2007/03/24 (version 0.2)</span>
<span class="comment">#          2007/04/09 (version 0.3)</span>
<span class="comment">#          2007/06/26 (version 0.4)</span>
<span class="comment">#          2008/04/05 (version 0.5)</span>
<span class="comment">#</span>
<span class="comment"># Description:</span>
<span class="comment"># ------------</span>
<span class="comment"># This is an easily customizable makefile template. The purpose is to</span>
<span class="comment"># provide an instant building environment for C/C++ programs.</span>
<span class="comment">#</span>
<span class="comment"># It searches all the C/C++ source files in the specified directories,</span>
<span class="comment"># makes dependencies, compiles and links to form an executable.</span>
<span class="comment">#</span>
<span class="comment"># Besides its default ability to build C/C++ programs which use only</span>
<span class="comment"># standard C/C++ libraries, you can customize the Makefile to build</span>
<span class="comment"># those using other libraries. Once done, without any changes you can</span>
<span class="comment"># then build programs using the same or less libraries, even if source</span>
<span class="comment"># files are renamed, added or removed. Therefore, it is particularly</span>
<span class="comment"># convenient to use it to build codes for experimental or study use.</span>
<span class="comment">#</span>
<span class="comment"># GNU make is expected to use the Makefile. Other versions of makes</span>
<span class="comment"># may or may not work.</span>
<span class="comment">#</span>
<span class="comment"># Usage:</span>
<span class="comment"># ------</span>
<span class="comment"># 1. Copy the Makefile to your program directory.</span>
<span class="comment"># 2. Customize in the "Customizable Section" only if necessary:</span>
<span class="comment">#    * to use non-standard C/C++ libraries, set pre-processor or compiler</span>
<span class="comment">#      options to &lt;MY_CFLAGS&gt; and linker ones to &lt;MY_LIBS&gt;</span>
<span class="comment">#      (See Makefile.gtk+-2.0 for an example)</span>
<span class="comment">#    * to search sources in more directories, set to &lt;SRCDIRS&gt;</span>
<span class="comment">#    * to specify your favorite program name, set to &lt;PROGRAM&gt;</span>
<span class="comment"># 3. Type make to start building your program.</span>
<span class="comment">#</span>
<span class="comment"># Make Target:</span>
<span class="comment"># ------------</span>
<span class="comment"># The Makefile provides the following targets to make:</span>
<span class="comment">#   $ make           compile and link</span>
<span class="comment">#   $ make NODEP=yes compile and link without generating dependencies</span>
<span class="comment">#   $ make objs      compile only (no linking)</span>
<span class="comment">#   $ make tags      create tags for Emacs editor</span>
<span class="comment">#   $ make ctags     create ctags for VI editor</span>
<span class="comment">#   $ make clean     clean objects and the executable file</span>
<span class="comment">#   $ make distclean clean objects, the executable and dependencies</span>
<span class="comment">#   $ make help      get the usage of the makefile</span>
<span class="comment">#</span>
<span class="comment">#===========================================================================</span>

<span class="comment">## Customizable Section: adapt those variables to suit your program.</span>
<span class="comment">##==========================================================================</span>

<span class="comment"># The pre-processor and compiler options.</span>
<span class="constant">MY_CFLAGS</span> =

<span class="comment"># The linker options.</span>
<span class="constant">MY_LIBS</span>   =

<span class="comment"># The pre-processor options used by the cpp (man cpp for more).</span>
<span class="comment"># CPPFLAGS  = -Wall</span>

<span class="comment"># The options used in linking as well as in any direct use of ld.</span>
<span class="constant">LDFLAGS</span>   =

<span class="comment"># The directories in which source files reside.</span>
<span class="comment"># If not specified, only the current directory will be serached.</span>
<span class="constant">SRCDIRS</span>   =

<span class="comment"># The executable file name.</span>
<span class="comment"># If not specified, current directory name or `a.out' will be used.</span>
<span class="constant">PROGRAM</span>   = test

<span class="comment">## Implicit Section: change the following only when necessary.</span>
<span class="comment">##==========================================================================</span>

<span class="comment"># The source file types (headers excluded).</span>
<span class="comment"># .c indicates C source files, and others C++ ones.</span>
<span class="constant">SRCEXTS</span> = .c .<span class="constant">C</span> .cc .cpp .<span class="constant">CPP</span> .c++ .cxx .cp

<span class="comment"># The header file types.</span>
<span class="constant">HDREXTS</span> = .h .<span class="constant">H</span> .hh .hpp .<span class="constant">HPP</span> .h++ .hxx .hp

<span class="comment"># The pre-processor and compiler options.</span>
<span class="comment"># Users can override those variables from the command line.</span>
<span class="comment"># CFLAGS  = -g -O2</span>
<span class="comment"># CXXFLAGS= -g -O2</span>

<span class="comment"># The C program compiler.</span>
<span class="comment"># CC     = gcc</span>

<span class="comment"># The C++ program compiler.</span>
<span class="comment">#CXX    = g++</span>

<span class="comment"># Un-comment the following line to compile C programs as C++ ones.</span>
<span class="comment">#CC     = $(CXX)</span>

<span class="comment"># The command used to delete file.</span>
<span class="comment">#RM     = rm -f</span>

<span class="constant">ETAGS</span> = etags
<span class="constant">ETAGSFLAGS</span> =

<span class="constant">CTAGS</span> = ctags
<span class="constant">CTAGSFLAGS</span> =

<span class="comment">## Stable Section: usually no need to be changed. But you can add more.</span>
<span class="comment">##==========================================================================</span>
<span class="constant">SHELL</span>   = <span class="regexp">/bin/sh</span>
<span class="constant">EMPTY</span>   =
<span class="constant">SPACE</span>   = <span class="variable">$(</span><span class="constant">EMPTY</span>) <span class="variable">$(</span><span class="constant">EMPTY</span>)
ifeq (<span class="variable">$(</span><span class="constant">PROGRAM</span>),)
  <span class="constant">CUR_PATH_NAMES</span> = <span class="variable">$(</span>subst /,<span class="variable">$(</span><span class="constant">SPACE</span>),<span class="variable">$(</span>subst <span class="variable">$(</span><span class="constant">SPACE</span>),<span class="number">_</span>,<span class="variable">$(</span><span class="constant">CURDIR</span>)))
  <span class="constant">PROGRAM</span> = <span class="variable">$(</span>word <span class="variable">$(</span>words <span class="variable">$(</span><span class="constant">CUR_PATH_NAMES</span>)),<span class="variable">$(</span><span class="constant">CUR_PATH_NAMES</span>))
  ifeq (<span class="variable">$(</span><span class="constant">PROGRAM</span>),)
    <span class="constant">PROGRAM</span> = a.out
  endif
endif
ifeq (<span class="variable">$(</span><span class="constant">SRCDIRS</span>),)
  <span class="constant">SRCDIRS</span> = .
endif
<span class="constant">SOURCES</span> = <span class="variable">$(</span>foreach d,<span class="variable">$(</span><span class="constant">SRCDIRS</span>),<span class="variable">$(</span>wildcard <span class="variable">$(</span>addprefix <span class="variable">$(</span>d)/*,<span class="variable">$(</span><span class="constant">SRCEXTS</span>))))
<span class="constant">HEADERS</span> = <span class="variable">$(</span>foreach d,<span class="variable">$(</span><span class="constant">SRCDIRS</span>),<span class="variable">$(</span>wildcard <span class="variable">$(</span>addprefix <span class="variable">$(</span>d)/*,<span class="variable">$(</span><span class="constant">HDREXTS</span>))))
<span class="constant">SRC_CXX</span> = <span class="variable">$(</span>filter-out %.c,<span class="variable">$(</span><span class="constant">SOURCES</span>))
<span class="constant">OBJS</span>    = <span class="variable">$(</span>addsuffix .o, <span class="variable">$(</span>basename <span class="variable">$(</span><span class="constant">SOURCES</span>)))
<span class="constant">DEPS</span>    = <span class="variable">$(</span><span class="constant">OBJS</span><span class="symbol">:</span>.o=.d)

<span class="comment">## Define some useful variables.</span>
<span class="constant">DEP_OPT</span> = <span class="variable">$(</span>shell <span class="keyword">if</span> <span class="string">`$(CC) --version | grep "GCC" &gt;/dev/null`</span>; <span class="keyword">then</span> \
                  echo <span class="string">"-MM -MP"</span>; <span class="keyword">else</span> echo <span class="string">"-M"</span>; fi )
<span class="constant">DEPEND</span>      = <span class="variable">$(</span><span class="constant">CC</span>)  <span class="variable">$(</span><span class="constant">DEP_OPT</span>)  <span class="variable">$(</span><span class="constant">MY_CFLAGS</span>) <span class="variable">$(</span><span class="constant">CFLAGS</span>) <span class="variable">$(</span><span class="constant">CPPFLAGS</span>)
<span class="constant">DEPEND</span>.d    = <span class="variable">$(</span>subst -g ,,<span class="variable">$(</span><span class="constant">DEPEND</span>))
<span class="constant">COMPILE</span>.c   = <span class="variable">$(</span><span class="constant">CC</span>)  <span class="variable">$(</span><span class="constant">MY_CFLAGS</span>) <span class="variable">$(</span><span class="constant">CFLAGS</span>)   <span class="variable">$(</span><span class="constant">CPPFLAGS</span>) -c
<span class="constant">COMPILE</span>.cxx = <span class="variable">$(</span><span class="constant">CXX</span>) <span class="variable">$(</span><span class="constant">MY_CFLAGS</span>) <span class="variable">$(</span><span class="constant">CXXFLAGS</span>) <span class="variable">$(</span><span class="constant">CPPFLAGS</span>) -c
<span class="constant">LINK</span>.c      = <span class="variable">$(</span><span class="constant">CC</span>)  <span class="variable">$(</span><span class="constant">MY_CFLAGS</span>) <span class="variable">$(</span><span class="constant">CFLAGS</span>)   <span class="variable">$(</span><span class="constant">CPPFLAGS</span>) <span class="variable">$(</span><span class="constant">LDFLAGS</span>)
<span class="constant">LINK</span>.cxx    = <span class="variable">$(</span><span class="constant">CXX</span>) <span class="variable">$(</span><span class="constant">MY_CFLAGS</span>) <span class="variable">$(</span><span class="constant">CXXFLAGS</span>) <span class="variable">$(</span><span class="constant">CPPFLAGS</span>) <span class="variable">$(</span><span class="constant">LDFLAGS</span>)

.<span class="constant">PHONY</span><span class="symbol">:</span> all objs tags ctags clean distclean help show

<span class="comment"># Delete the default suffixes</span>
.<span class="constant">SUFFIXES</span><span class="symbol">:</span>

<span class="symbol">all:</span> <span class="variable">$(</span><span class="constant">PROGRAM</span>)

<span class="comment"># Rules for creating dependency files (.d).</span>
<span class="comment">#------------------------------------------</span>

%.<span class="symbol">d:</span>%.c
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.<span class="constant">C</span>
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.cc
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.cpp
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.<span class="constant">CPP</span>
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.c++
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.cp
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

%.<span class="symbol">d:</span>%.cxx
    <span class="variable">@echo</span> -n <span class="variable">$(</span>dir <span class="variable">$&lt;</span>) &gt; <span class="variable">$@</span>
    @<span class="variable">$(</span><span class="constant">DEPEND</span>.d) <span class="variable">$&lt;</span> <span class="prompt">&gt;&gt; </span><span class="variable">$@</span>

<span class="comment"># Rules for generating object files (.o).</span>
<span class="comment">#----------------------------------------</span>
<span class="symbol">objs:</span><span class="variable">$(</span><span class="constant">OBJS</span>)

%.<span class="symbol">o:</span>%.c
    <span class="variable">$(</span><span class="constant">COMPILE</span>.c) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.<span class="constant">C</span>
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.cc
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.cpp
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.<span class="constant">CPP</span>
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.c++
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.cp
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

%.<span class="symbol">o:</span>%.cxx
    <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx) <span class="variable">$&lt;</span> -o <span class="variable">$@</span>

<span class="comment"># Rules for generating the tags.</span>
<span class="comment">#-------------------------------------</span>
<span class="symbol">tags:</span> <span class="variable">$(</span><span class="constant">HEADERS</span>) <span class="variable">$(</span><span class="constant">SOURCES</span>)
    <span class="variable">$(</span><span class="constant">ETAGS</span>) <span class="variable">$(</span><span class="constant">ETAGSFLAGS</span>) <span class="variable">$(</span><span class="constant">HEADERS</span>) <span class="variable">$(</span><span class="constant">SOURCES</span>)

<span class="symbol">ctags:</span> <span class="variable">$(</span><span class="constant">HEADERS</span>) <span class="variable">$(</span><span class="constant">SOURCES</span>)
    <span class="variable">$(</span><span class="constant">CTAGS</span>) <span class="variable">$(</span><span class="constant">CTAGSFLAGS</span>) <span class="variable">$(</span><span class="constant">HEADERS</span>) <span class="variable">$(</span><span class="constant">SOURCES</span>)

<span class="comment"># Rules for generating the executable.</span>
<span class="comment">#-------------------------------------</span>
<span class="variable">$(</span><span class="constant">PROGRAM</span>)<span class="symbol">:</span><span class="variable">$(</span><span class="constant">OBJS</span>)
ifeq (<span class="variable">$(</span><span class="constant">SRC_CXX</span>),)              <span class="comment"># C program</span>
    <span class="variable">$(</span><span class="constant">LINK</span>.c)   <span class="variable">$(</span><span class="constant">OBJS</span>) <span class="variable">$(</span><span class="constant">MY_LIBS</span>) -o <span class="variable">$@</span>
    <span class="variable">@echo</span> <span class="constant">Type</span> ./<span class="variable">$@</span> to execute the program.
<span class="keyword">else</span>                            <span class="comment"># C++ program</span>
    <span class="variable">$(</span><span class="constant">LINK</span>.cxx) <span class="variable">$(</span><span class="constant">OBJS</span>) <span class="variable">$(</span><span class="constant">MY_LIBS</span>) -o <span class="variable">$@</span>
    <span class="variable">@echo</span> <span class="constant">Type</span> ./<span class="variable">$@</span> to execute the program.
endif

ifndef <span class="constant">NODEP</span>
ifneq (<span class="variable">$(</span><span class="constant">DEPS</span>),)
  sinclude <span class="variable">$(</span><span class="constant">DEPS</span>)
endif
endif

<span class="symbol">clean:</span>
    <span class="variable">$(</span><span class="constant">RM</span>) <span class="variable">$(</span><span class="constant">OBJS</span>) <span class="variable">$(</span><span class="constant">PROGRAM</span>) <span class="variable">$(</span><span class="constant">PROGRAM</span>).exe

<span class="symbol">distclean:</span> clean
    <span class="variable">$(</span><span class="constant">RM</span>) <span class="variable">$(</span><span class="constant">DEPS</span>) <span class="constant">TAGS</span>

<span class="comment"># Show help.</span>
<span class="symbol">help:</span>
    <span class="variable">@echo</span> <span class="string">'Generic Makefile for C/C++ Programs (gcmakefile) version 0.5'</span>
    <span class="variable">@echo</span> <span class="string">'Copyright (C) 2007, 2008 whyglinux &lt;whyglinux@hotmail.com&gt;'</span>
    <span class="variable">@echo</span>
    <span class="variable">@echo</span> <span class="string">'Usage: make [TARGET]'</span>
    <span class="variable">@echo</span> <span class="string">'TARGETS:'</span>
    <span class="variable">@echo</span> <span class="string">'  all       (=make) compile and link.'</span>
    <span class="variable">@echo</span> <span class="string">'  NODEP=yes make without generating dependencies.'</span>
    <span class="variable">@echo</span> <span class="string">'  objs      compile only (no linking).'</span>
    <span class="variable">@echo</span> <span class="string">'  tags      create tags for Emacs editor.'</span>
    <span class="variable">@echo</span> <span class="string">'  ctags     create ctags for VI editor.'</span>
    <span class="variable">@echo</span> <span class="string">'  clean     clean objects and the executable file.'</span>
    <span class="variable">@echo</span> <span class="string">'  distclean clean objects, the executable and dependencies.'</span>
    <span class="variable">@echo</span> <span class="string">'  show      show variables (for debug use only).'</span>
    <span class="variable">@echo</span> <span class="string">'  help      print this message.'</span>
    <span class="variable">@echo</span>
    <span class="variable">@echo</span> <span class="string">'Report bugs to &lt;whyglinux AT gmail DOT com&gt;.'</span>

<span class="comment"># Show variables (for debug use only.)</span>
<span class="symbol">show:</span>
    <span class="variable">@echo</span> <span class="string">'PROGRAM     :'</span> <span class="variable">$(</span><span class="constant">PROGRAM</span>)
    <span class="variable">@echo</span> <span class="string">'SRCDIRS     :'</span> <span class="variable">$(</span><span class="constant">SRCDIRS</span>)
    <span class="variable">@echo</span> <span class="string">'HEADERS     :'</span> <span class="variable">$(</span><span class="constant">HEADERS</span>)
    <span class="variable">@echo</span> <span class="string">'SOURCES     :'</span> <span class="variable">$(</span><span class="constant">SOURCES</span>)
    <span class="variable">@echo</span> <span class="string">'SRC_CXX     :'</span> <span class="variable">$(</span><span class="constant">SRC_CXX</span>)
    <span class="variable">@echo</span> <span class="string">'OBJS        :'</span> <span class="variable">$(</span><span class="constant">OBJS</span>)
    <span class="variable">@echo</span> <span class="string">'DEPS        :'</span> <span class="variable">$(</span><span class="constant">DEPS</span>)
    <span class="variable">@echo</span> <span class="string">'DEPEND      :'</span> <span class="variable">$(</span><span class="constant">DEPEND</span>)
    <span class="variable">@echo</span> <span class="string">'COMPILE.c   :'</span> <span class="variable">$(</span><span class="constant">COMPILE</span>.c)
    <span class="variable">@echo</span> <span class="string">'COMPILE.cxx :'</span> <span class="variable">$(</span><span class="constant">COMPILE</span>.cxx)
    <span class="variable">@echo</span> <span class="string">'link.c      :'</span> <span class="variable">$(</span><span class="constant">LINK</span>.c)
    <span class="variable">@echo</span> <span class="string">'link.cxx    :'</span> <span class="variable">$(</span><span class="constant">LINK</span>.cxx)

<span class="comment">## End of the Makefile ##  Suggestions are welcome  ## All rights reserved ##</span>
<span class="comment">##############################################################</span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 感谢whyglinux大大！然后上Sublime的配置C C++ Multiple Files.sublime-build：</p>
<pre><code>{
    "<span class="attribute">shell_cmd</span>": <span class="value"><span class="string">"make"</span></span>,
    "<span class="attribute">file_regex</span>": <span class="value"><span class="string">"^(..[^:\n]*):([0-9]+):?([0-9]+)?:? (.*)$"</span></span>,
    "<span class="attribute">working_dir</span>": <span class="value"><span class="string">"${folder:${project_path:${file_path}}}"</span></span>,
    "<span class="attribute">selector</span>": <span class="value"><span class="string">"source.c, source.cpp"</span></span>,
    "<span class="attribute">syntax</span>": <span class="value"><span class="string">"Packages/Makefile/Make.build-language"</span></span>,
    "<span class="attribute">keyfiles</span>": <span class="value">[<span class="string">"Makefile"</span>, <span class="string">"makefile"</span>]</span>,

    "<span class="attribute">variants</span>":
    <span class="value">[
        {
            "<span class="attribute">name</span>": <span class="value"><span class="string">"Clean"</span></span>,
            "<span class="attribute">shell_cmd</span>": <span class="value"><span class="string">"make clean"</span>
        </span>},
        {
            "<span class="attribute">name</span>": <span class="value"><span class="string">"Run"</span></span>,
            "<span class="attribute">shell_cmd</span>": <span class="value"><span class="string">"${file_path}/${file_base_name}"</span>
        </span>}
    ]
</span>}
</code></pre><p>&nbsp;&nbsp;&nbsp; 你们大可以配成执行时自动弹出终端，这样还能交互。我为了解决Sublime里中文输入法的问题用了gtk2，但是gnome-terminal用的是gtk3，不兼容了。写写控制台程序也不用那么多交互，懒得折腾了，你们随意。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 不自己动手为Sublime配个C/C++的通用Makefile脚本吗？不能自动编译链接多文件的编辑器不是好IDE。<br>]]>
    
    </summary>
    
      <category term="C" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="C++" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="makefile" scheme="http://blog.bipedalbit.net/tags/makefile/"/>
    
      <category term="sublime" scheme="http://blog.bipedalbit.net/tags/sublime/"/>
    
      <category term="Sublime Text" scheme="http://blog.bipedalbit.net/categories/Sublime-Text/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[【四】nginx的数据结构(2)——自己动手重写红黑树]]></title>
    <link href="http://blog.bipedalbit.net/2015/8/16/%E3%80%90%E5%9B%9B%E3%80%91nginx%E7%9A%84%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-2-%E2%80%94%E2%80%94%E8%87%AA%E5%B7%B1%E5%8A%A8%E6%89%8B%E9%87%8D%E5%86%99%E7%BA%A2%E9%BB%91%E6%A0%91/"/>
    <id>http://blog.bipedalbit.net/2015/8/16/【四】nginx的数据结构-2-——自己动手重写红黑树/</id>
    <published>2015-08-16T14:06:30.000Z</published>
    <updated>2015-09-16T04:17:40.679Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 不管看了多少遍有多滚瓜烂熟，数据结构或者算法的代码如果不亲自码一遍，我总是不敢说自己懂了。这次就来自己动手重写红黑树。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; 费话不多说，上重写代码，这次姑且用英语写的注释当复习英语了。<br>&nbsp;&nbsp;&nbsp; rbtree.h：</p>
<pre><code><span class="comment">/*
 * Copyright (C) Bipedal Bit
 * Verson 1.0.0.1
 */</span>

<span class="preprocessor">#<span class="keyword">ifndef</span> _RBTREE_H_INCLUDED_</span>
<span class="preprocessor">#<span class="keyword">define</span> _RBTREE_H_INCLUDED_</span>

<span class="comment">/* the node structure of the red-black tree */</span>
<span class="keyword">typedef</span> <span class="keyword">struct</span> rbtree_node_s <span class="keyword">rbtree_node_t</span>;
<span class="comment">/* Using type int means its range is -0x7fffffff-1~0x7fffffff. */</span>
<span class="keyword">typedef</span> <span class="keyword">int</span> <span class="keyword">rbtree_key_t</span>;
<span class="comment">/* Abstract type is complicated to achieve with C so I use char* instead. */</span>
<span class="keyword">typedef</span> <span class="keyword">char</span>* <span class="keyword">rbtree_data_t</span>;

<span class="keyword">struct</span> rbtree_node_s
{
    <span class="comment">/* key of the node */</span>
    <span class="keyword">rbtree_key_t</span>    key;
    <span class="comment">/* pointer of the parent of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    parent;
    <span class="comment">/* pointer of the left kid of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    left;
    <span class="comment">/* pointer of the right kid of the node */</span>
    <span class="keyword">rbtree_node_t</span>*    right;
    <span class="comment">/* color of the node */</span>
    <span class="keyword">unsigned</span> <span class="keyword">char</span>    color;
    <span class="comment">/* pointer of the value of the node corresponding to the key */</span>
    <span class="keyword">rbtree_data_t</span>    value;
};

<span class="comment">/* the tree object stucture of the red-black tree */</span>
<span class="keyword">typedef</span> <span class="keyword">struct</span> rbtree_s <span class="keyword">rbtree_t</span>;
<span class="comment">/* foundational insert function pointer*/</span>
<span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*rbtree_insert_p)</span> <span class="params">(rbtree_t* root, rbtree_node_t* node)</span></span>;

<span class="keyword">struct</span> rbtree_s
{
    <span class="comment">/* the pointer of the root node of the tree */</span>
    <span class="keyword">rbtree_node_t</span>* root;
    <span class="comment">/* black leaf nodes as sentinel */</span>
    <span class="keyword">rbtree_node_t</span>* sentinel;
    <span class="comment">/* the polymorphic insert function pointer */</span>
    rbtree_insert_p insert;
};

<span class="comment">/* macros */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_init(tree, s, i)        \
rbtree_sentinel_init(s);            \
(tree)-&gt;root = s;                \
(tree)-&gt;sentinel = s;            \
(tree)-&gt;insert = i</span>

<span class="preprocessor">#<span class="keyword">define</span> rbtree_red(node)    ((node)-&gt;color = <span class="number">1</span>)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_black(node)    ((node)-&gt;color = <span class="number">0</span>)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_is_red(node)    ((node)-&gt;color)</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_is_black(node)    (!rbtree_is_red(node))</span>
 <span class="comment">/* copy n2's color to n1 */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_copy_color(n1, n2)    (n1-&gt;color = n2-&gt;color)</span>
<span class="comment">/* sentinel must be black cuz it's leaf node */</span>
<span class="preprocessor">#<span class="keyword">define</span> rbtree_sentinel_init(node)    rbtree_black(node)</span>

<span class="comment">/* statements of public methods */</span>
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_insert_value</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_insert</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="function"><span class="keyword">void</span> <span class="title">rbtree_delete</span><span class="params">(rbtree_t* tree, rbtree_node_t* node)</span></span>;
<span class="keyword">rbtree_node_t</span>* rbtree_find(<span class="keyword">rbtree_t</span>* tree, <span class="keyword">rbtree_key_t</span> key);

<span class="preprocessor">#<span class="keyword">endif</span>    <span class="comment">/* _RBTREE_H_INCLUDED_ */</span></span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 看过nginx源码的有心人会发现，我的头文件相对于ngx_rbree.h改动不大，非常像。<br>&nbsp;&nbsp;&nbsp; 关键的rbtree.c：</p>
<pre><code><span class="comment">/*
 * Copyright (C) Bipedal Bit
 * Verson 1.0.0.1
 */</span>

<span class="variable">#include</span> &lt;stddef<span class="built_in">.</span>h&gt;
<span class="variable">#include</span> <span class="string">"rbtree.h"</span>

<span class="comment">/* inline methods */</span>
<span class="comment">/* get the node with the minimum key in a subtree of the red-black tree */</span>
static <span class="keyword">inline</span> rbtree_node_t*
rbtree_subtree_min(rbtree_node_t* node, rbtree_node_t* sentinel)
{
    <span class="keyword">while</span>(node<span class="subst">-&gt;</span>left != sentinel)
    {
        node = node<span class="subst">-&gt;</span>left;
    }

    <span class="keyword">return</span> node;
}

<span class="comment">/* replace the node "node" in the tree with node "tmp" */</span>
static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_replace(rbtree_t* tree,
    rbtree_node_t* node, rbtree_node_t* tmp)
{
    <span class="comment">/* upward: p[node] &lt;- p[tmp] */</span>
    tmp<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;

    <span class="keyword">if</span> (node == tree<span class="subst">-&gt;</span>root)
    {
        tree<span class="subst">-&gt;</span>root = tmp;
    }
    <span class="keyword">else</span> <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
    {
        <span class="comment">/* downward: left[p[node]] &lt;- tmp */</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = tmp;
    }
    <span class="keyword">else</span>
    {
        <span class="comment">/* downward: right[p[node]] &lt;- tmp */</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = tmp;
    }

    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = tmp;
}

<span class="comment">/* change the topologic structure of the tree keeping the order of the nodes */</span>
static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_left_rotate(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="comment">/* node as the var x in CLRS while tmp as the var y */</span>
    rbtree_node_t* tmp = node<span class="subst">-&gt;</span>right;

    <span class="comment">/* replace y with left[y] */</span>
    <span class="comment">/* downward: right[x] &lt;- left[y] */</span>
    node<span class="subst">-&gt;</span>right = tmp<span class="subst">-&gt;</span>left;
    <span class="comment">/* if left[[y] is not NIL it has a parent */</span>
    <span class="keyword">if</span> (tmp<span class="subst">-&gt;</span>left != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="comment">/* upward: p[left[y]] &lt;- x */</span>
        tmp<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span><span class="keyword">parent</span> = node;
    }

    <span class="comment">/* replace x with y */</span>
    rbtree_replace(tree, node, tmp);
    tmp<span class="subst">-&gt;</span>left = node;
}

static <span class="keyword">inline</span> <span class="literal">void</span> rbtree_right_rotate(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* tmp = node<span class="subst">-&gt;</span>left;

    <span class="comment">/* replace y with right[y] */</span>
    node<span class="subst">-&gt;</span>left = tmp<span class="subst">-&gt;</span>right;
    <span class="keyword">if</span> (tmp<span class="subst">-&gt;</span>right != tree<span class="subst">-&gt;</span>sentinel)
    {
        tmp<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span><span class="keyword">parent</span> = node;
    }

    <span class="comment">/* replace x with y */</span>
    rbtree_replace(tree, node, tmp);
    tmp<span class="subst">-&gt;</span>right = node;
}

<span class="comment">/* static methods */</span>
<span class="comment">/* fix the red-black tree after the new node inserted */</span>
static <span class="literal">void</span> rbtree_insert_fixup(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="keyword">while</span>(rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>))
    {
        <span class="keyword">if</span> (node<span class="subst">-&gt;</span><span class="keyword">parent</span> == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            <span class="comment">/* case 1: node's uncle is red */</span>
            <span class="keyword">if</span> (rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right))
            {
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;
                <span class="comment">/* Then we can consider the whole subtree */</span>
                <span class="comment">/* which is represented by the new "node" as the "node" before */</span>
                <span class="comment">/* and keep looping till "node" become the root. */</span>
            }
            <span class="comment">/* case 2: node's uncle is black */</span>
            <span class="keyword">else</span>
            {
                <span class="comment">/* ensure node is the left kid of its parent */</span>
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right)
                {
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
                    rbtree_left_rotate(tree, node);
                }
                <span class="comment">/* case 2 -&gt; case 1 */</span>
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_right_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
            }
        }
        <span class="comment">/* same as the "if" clause before with "left" and "right" exchanged */</span>
        <span class="keyword">else</span>
        {
            <span class="keyword">if</span> (rbtree_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left))
            {
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
                {
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
                    rbtree_right_rotate(tree, node);
                }
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
            }
        }
    }
    <span class="comment">/* ensure the root node being black */</span>
    rbtree_black(tree<span class="subst">-&gt;</span>root);
}

static <span class="literal">void</span> rbtree_delete_fixup(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* brother = <span class="built_in">NULL</span>;

    <span class="keyword">while</span>(node != tree<span class="subst">-&gt;</span>root <span class="subst">&amp;&amp;</span> rbtree_is_black(node))
    {
        <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
            <span class="keyword">if</span> (rbtree_is_red(brother))
            {
                rbtree_black(brother);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                <span class="comment">/* update brother after topologic change of the tree */</span>
                brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
            }

            <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> rbtree_is_black(brother<span class="subst">-&gt;</span>right))
            {
                rbtree_red(brother);
                <span class="comment">/* go upward and keep on fixing color */</span>
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>right))
                {
                    rbtree_black(brother<span class="subst">-&gt;</span>left);
                    rbtree_red(brother);
                    rbtree_right_rotate(tree, brother);
                    <span class="comment">/* update brother after topologic change of the tree */</span>
                    brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
                }
                rbtree_copy_col<span class="subst">or</span>(brother, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(brother<span class="subst">-&gt;</span>right);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                <span class="comment">/* end the loop and ensure root is black */</span>
                node = tree<span class="subst">-&gt;</span>root;
            }
        }
        <span class="comment">/* same as the "if" clause before with "left" and "right" exchanged */</span>
        <span class="keyword">else</span>
        {
            brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            <span class="keyword">if</span> (rbtree_is_red(brother))
            {
                rbtree_black(brother);
                rbtree_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            }

            <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> rbtree_is_black(brother<span class="subst">-&gt;</span>right))
            {
                rbtree_red(brother);
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
            }
            <span class="keyword">else</span>
            {
                <span class="keyword">if</span> (rbtree_is_black(brother<span class="subst">-&gt;</span>left))
                {
                    rbtree_black(brother<span class="subst">-&gt;</span>right);
                    rbtree_red(brother);
                    rbtree_right_rotate(tree, brother);
                    brother = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
                }
                rbtree_copy_col<span class="subst">or</span>(brother, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                rbtree_black(brother<span class="subst">-&gt;</span>left);
                rbtree_left_rotate(tree, node<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                node = tree<span class="subst">-&gt;</span>root;
            }
        }
    }

    rbtree_black(node);
}

<span class="comment">/* public methods */</span>
<span class="literal">void</span> rbtree_insert_value(rbtree_t* tree, rbtree_node_t* node)
{
    <span class="comment">/* Using ** to know wether the new node will be a left kid */</span>
    <span class="comment">/* or a right kid of its parent node. */</span>
    rbtree_node_t** tmp = <span class="subst">&amp;</span>tree<span class="subst">-&gt;</span>root;
    rbtree_node_t* <span class="keyword">parent</span>;

    <span class="keyword">while</span>(*tmp != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="keyword">parent</span> = *tmp;
        tmp = (node<span class="subst">-&gt;</span>key &lt; <span class="keyword">parent</span><span class="subst">-&gt;</span>key) ? <span class="subst">&amp;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left : <span class="subst">&amp;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
    }

    <span class="comment">/* The pointer knows wether the node should be on the left side */</span>
    <span class="comment">/* or on the right one. */</span>
    *tmp = node;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="keyword">parent</span>;
    node<span class="subst">-&gt;</span>left = tree<span class="subst">-&gt;</span>sentinel;
    node<span class="subst">-&gt;</span>right = tree<span class="subst">-&gt;</span>sentinel;
    rbtree_red(node);
}

<span class="literal">void</span> rbtree_insert(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* sentinel = tree<span class="subst">-&gt;</span>sentinel;

    <span class="comment">/* if the tree is empty */</span>
    <span class="keyword">if</span> (tree<span class="subst">-&gt;</span>root == sentinel)
    {
        tree<span class="subst">-&gt;</span>root = node;
        node<span class="subst">-&gt;</span><span class="keyword">parent</span> = sentinel;
        node<span class="subst">-&gt;</span>left = sentinel;
        node<span class="subst">-&gt;</span>right = sentinel;
        rbtree_black(node);

        <span class="keyword">return</span>;
    }

    <span class="comment">/* generally */</span>
    tree<span class="subst">-&gt;</span>insert(tree, node);
    rbtree_insert_fixup(tree, node);
}

<span class="literal">void</span> rbtree_delete(rbtree_t* tree, rbtree_node_t* node)
{
    rbtree_node_t* sentinel = tree<span class="subst">-&gt;</span>sentinel;
    <span class="comment">/* wether "node" is on the left side or the right one */</span>
    rbtree_node_t** ptr_to_node = <span class="built_in">NULL</span>;
    <span class="comment">/* "cover" is the node which is going to cover "node" */</span>
    rbtree_node_t* cover = <span class="built_in">NULL</span>;
    <span class="comment">/* wether we lossing a red node on the edge of the tree */</span>
    int loss_red = rbtree_is_red(node);
    int is_root = (node == tree<span class="subst">-&gt;</span>root);

    <span class="comment">/* get "cover" &amp; "loss_red"  */</span>
    <span class="comment">/* sentinel in "node"'s kids */</span>
    <span class="keyword">if</span> (node<span class="subst">-&gt;</span>left == sentinel)
    {
        cover = node<span class="subst">-&gt;</span>right;
    }
    <span class="keyword">else</span> <span class="keyword">if</span> (node<span class="subst">-&gt;</span>right == sentinel)
    {
        cover = node<span class="subst">-&gt;</span>left;
    }
    <span class="comment">/* "node"'s kids are both non-sentinel */</span>
    <span class="keyword">else</span>
    {
        <span class="comment">/* update "node" &amp; "loss_red" &amp; "is_root" &amp; "cover" */</span>
        cover = rbtree_subtree_min(node<span class="subst">-&gt;</span>right, sentinel);
        node<span class="subst">-&gt;</span>key = cover<span class="subst">-&gt;</span>key;
        node<span class="subst">-&gt;</span>value = cover<span class="subst">-&gt;</span>value;
        node = cover;
        loss_red = rbtree_is_red(node);
        is_root = <span class="number">0</span>;
        <span class="comment">/* move "cover"'s kids */</span>
        <span class="comment">/* "cover" can only be a left kid */</span>
        <span class="comment">/* and can only have a right non-sentinel kid */</span>
        <span class="comment">/* because of function "rbtree_subtree_min" */</span>
        cover = node<span class="subst">-&gt;</span>right;
    }

    <span class="keyword">if</span> (is_root)
    {
        <span class="comment">/* update root */</span>
        tree<span class="subst">-&gt;</span>root = cover;
    }
    <span class="keyword">else</span>
    {
        <span class="comment">/* downward link */</span>
        <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left)
        {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = cover;
        }
        <span class="keyword">else</span>
        {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = cover;
        }
    }
    <span class="comment">/* upward link */</span>
    cover<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
    <span class="comment">/* "cover" may be a sentinel */</span>
    <span class="keyword">if</span> (cover != sentinel)
    {
        <span class="comment">/* set "cover" */</span>
        cover<span class="subst">-&gt;</span>left = node<span class="subst">-&gt;</span>left;
        cover<span class="subst">-&gt;</span>right = node<span class="subst">-&gt;</span>right;
        rbtree_copy_col<span class="subst">or</span>(cover, node);
    }

    <span class="comment">/* clear "node" since it's useless */</span>
    node<span class="subst">-&gt;</span>key = -<span class="number">1</span>;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>left = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>right = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>value = <span class="built_in">NULL</span>;

    <span class="keyword">if</span> (loss_red)
    {
        <span class="keyword">return</span>;
    }

    <span class="comment">/* When lossing a black node on edge */</span>
    <span class="comment">/* the fifth rule of red-black tree will be broke. */</span>
    <span class="comment">/* So the tree need to be fixed. */</span>
    rbtree_delete_fixup(tree, cover);
}

<span class="comment">/* find the node in the tree corresponding to the given key value */</span>
rbtree_node_t* rbtree_find(rbtree_t* tree, rbtree_key_t key)
{
    rbtree_node_t* tmp = tree<span class="subst">-&gt;</span>root;
    int step_cnt = <span class="number">0</span>;

    <span class="comment">/* search the binary tree */</span>
    <span class="keyword">while</span>(tmp != tree<span class="subst">-&gt;</span>sentinel)
    {
        <span class="comment">/* next line is just fot test */</span>
        <span class="comment">// step_cnt++;</span>
        <span class="keyword">if</span>(key == tmp<span class="subst">-&gt;</span>key)
        {
            <span class="comment">/* next line is just for test */</span>
            <span class="comment">// printf("step count: %d, color: %s, ", step_cnt, rbtree_is_red(tmp) ? "red" : "black");</span>
            <span class="keyword">return</span> tmp;
        }

        tmp = (key &lt; tmp<span class="subst">-&gt;</span>key) ? tmp<span class="subst">-&gt;</span>left : tmp<span class="subst">-&gt;</span>right;
    }

    <span class="keyword">return</span> <span class="built_in">NULL</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp;  虽然明白nginx源码中100+行的长函数体也是一种避免太多函数调用增加时间空间开销的优化，我还是把所有函数都分类分割成100行以下。增加可读性是一方面，可能也是有点强迫症吧。之后会扩展几个统计方法，像max、min和mid，还会扩展一个遍历方法。<br>&nbsp;&nbsp;&nbsp; 下面是调用测试，test.c：</p>
<pre><code><span class="preprocessor">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span>
<span class="preprocessor">#<span class="keyword">include</span> <span class="string">"rbtree.h"</span></span>

<span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span>
</span>{
    <span class="keyword">rbtree_t</span> t = {};
    <span class="keyword">rbtree_node_t</span> s = {};
    rbtree_init(&amp;t, &amp;s, rbtree_insert_value);

    <span class="keyword">const</span> <span class="keyword">int</span> cnt = <span class="number">10</span>;
    <span class="keyword">const</span> <span class="keyword">int</span> max_len = <span class="number">15</span>;

<span class="preprocessor">#<span class="keyword">define</span> TEST_VALUES {<span class="string">"apple"</span>, <span class="string">"banana"</span>, <span class="string">"cherry"</span>, <span class="string">"grape"</span>, <span class="string">"lemon"</span>, <span class="string">"mango"</span>, <span class="string">"pear"</span>, <span class="string">"pineapple"</span>, <span class="string">"strawberry"</span>, <span class="string">"watermelon"</span>}</span>

    <span class="comment">/* for gcc */</span>
    <span class="keyword">char</span>* v[] = TEST_VALUES;
    <span class="comment">/* for g++ */</span>
    <span class="comment">// char v[][max_len] = TEST_VALUES;</span>

    <span class="keyword">rbtree_node_t</span> n[cnt];
    <span class="keyword">int</span> i;
    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; cnt; i++)
    {
        n[i].key = i+<span class="number">1</span>;
        n[i].value = v[i];
        rbtree_insert(&amp;t, &amp;n[i]);
    }

    <span class="keyword">rbtree_node_t</span>* p[cnt];

    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt;= cnt; i++)
    {
        <span class="built_in">printf</span>(<span class="string">"key: %d\n"</span>, i);
        p[i] = rbtree_find(&amp;t, i);
        <span class="built_in">printf</span>(<span class="string">"value: %s\n"</span>, (p[i] != <span class="literal">NULL</span>) ? p[i]-&gt;value : <span class="string">"?"</span>);
    }

    rbtree_delete(&amp;t, &amp;n[<span class="number">5</span>]);

    <span class="built_in">printf</span>(<span class="string">"\nafter delete 6-&gt;mango:\n\n"</span>);

    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt;= cnt; i++)
    {
        <span class="built_in">printf</span>(<span class="string">"key: %d\n"</span>, i);
        p[i] = rbtree_find(&amp;t, i);
        <span class="built_in">printf</span>(<span class="string">"value: %s\n"</span>, (p[i] != <span class="literal">NULL</span>) ? p[i]-&gt;value : <span class="string">"?"</span>);
    }

    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 解开rbtree_find方法里的测试行注释，顺利执行：</p>
<pre><code><span class="attribute">key</span>: <span class="number">1</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: apple
<span class="attribute">key</span>: <span class="number">2</span>
step <span class="attribute">count</span>: <span class="number">2</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: banana
<span class="attribute">key</span>: <span class="number">3</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: cherry
<span class="attribute">key</span>: <span class="number">4</span>
step <span class="attribute">count</span>: <span class="number">1</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: grape
<span class="attribute">key</span>: <span class="number">5</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: lemon
<span class="attribute">key</span>: <span class="number">6</span>
step <span class="attribute">count</span>: <span class="number">2</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: mango
<span class="attribute">key</span>: <span class="number">7</span>
step <span class="attribute">count</span>: <span class="number">4</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: pear
<span class="attribute">key</span>: <span class="number">8</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: red, <span class="attribute">value</span>: pineapple
<span class="attribute">key</span>: <span class="number">9</span>
step <span class="attribute">count</span>: <span class="number">4</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: strawberry
<span class="attribute">key</span>: <span class="number">10</span>
step <span class="attribute">count</span>: <span class="number">5</span>, <span class="attribute">color</span>: red, <span class="attribute">value</span>: watermelon

after delete <span class="number">6</span>-&gt;<span class="attribute">mango</span>:

<span class="attribute">key</span>: <span class="number">1</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: apple
<span class="attribute">key</span>: <span class="number">2</span>
step <span class="attribute">count</span>: <span class="number">2</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: banana
<span class="attribute">key</span>: <span class="number">3</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: cherry
<span class="attribute">key</span>: <span class="number">4</span>
step <span class="attribute">count</span>: <span class="number">1</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: grape
<span class="attribute">key</span>: <span class="number">5</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: lemon
<span class="attribute">key</span>: <span class="number">6</span>
<span class="attribute">value</span>: ?
<span class="attribute">key</span>: <span class="number">7</span>
step <span class="attribute">count</span>: <span class="number">2</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: pear
<span class="attribute">key</span>: <span class="number">8</span>
step <span class="attribute">count</span>: <span class="number">4</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: pineapple
<span class="attribute">key</span>: <span class="number">9</span>
step <span class="attribute">count</span>: <span class="number">3</span>, <span class="attribute">color</span>: red, <span class="attribute">value</span>: strawberry
<span class="attribute">key</span>: <span class="number">10</span>
step <span class="attribute">count</span>: <span class="number">4</span>, <span class="attribute">color</span>: black, <span class="attribute">value</span>: watermelon
</code></pre><p>&nbsp;&nbsp;&nbsp; 下面分别是删除6-&gt;mango前的红黑树和删除后的红黑树示意图：<br><img src="http://img.blog.csdn.net/20150816200143108" alt="" height="422" width="400"><br><img src="http://img.blog.csdn.net/20150816200200052" alt="" height="331" width="400"><br>&nbsp;&nbsp;&nbsp; 下面我们来做个大量数据的压力测试，注意把rbtree_find方法里的测试行注释掉，不然后果恐怕会比较吓人：</p>
<pre><code><span class="preprocessor">#include <span class="title">&lt;stdio.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">&lt;stdlib.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">&lt;time.h&gt;</span></span>
<span class="preprocessor">#include <span class="title">"rbtree.h"</span></span>

<span class="keyword">int</span> main(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])
{
    <span class="keyword">double</span> duration;
    <span class="keyword">double</span> room;

    rbtree_t t = {};
    rbtree_node_t s = {};
    rbtree_init(&amp;t, &amp;s, rbtree_insert_value);

    <span class="keyword">const</span> <span class="keyword">int</span> cnt = <span class="number">1</span>&lt;&lt;<span class="number">20</span>;
    <span class="keyword">const</span> <span class="keyword">int</span> max_len = <span class="number">15</span>;

<span class="preprocessor">#define TEST_VALUES {<span class="title">"apple"</span>, <span class="title">"banana"</span>, <span class="title">"cherry"</span>, <span class="title">"grape"</span>, <span class="title">"lemon"</span>, <span class="title">"mango"</span>, <span class="title">"pear"</span>, <span class="title">"pineapple"</span>, <span class="title">"strawberry"</span>, <span class="title">"watermelon"</span>}</span>

    <span class="comment">/* for gcc */</span>
    <span class="keyword">char</span>* v[] = TEST_VALUES;
    <span class="comment">/* for g++ */</span>
    <span class="comment">// char v[][max_len] = TEST_VALUES;</span>

    <span class="comment">/* Default stack size in Ubuntu Kylin 14.04 is 8MB. */</span>
    <span class="comment">/* It's not enough. So I use memory in heap which offers a lot larger room. */</span>
    rbtree_node_t* n = (rbtree_node_t*)calloc(cnt, <span class="keyword">sizeof</span>(rbtree_node_t));
    <span class="keyword">int</span> i;

    <span class="keyword">long</span> time1 = clock();

    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; cnt; i++)
    {
        n[i]<span class="variable">.key</span> = i+<span class="number">1</span>;
        n[i]<span class="variable">.value</span> = v[i%<span class="number">10</span>];
        rbtree_insert(&amp;t, &amp;n[i]);
    }

    <span class="keyword">long</span> time2 = clock();
    room = <span class="number">48.0</span>*cnt/(<span class="number">1</span>&lt;&lt;<span class="number">20</span>);
    duration = (<span class="keyword">double</span>)(time2 - time1) / CLOCKS_PER_SEC;
    printf(<span class="string">"Inserting %d nodes costs %.2fMB and spends %f seconds.\n"</span>, cnt, room, duration);

    <span class="keyword">const</span> <span class="keyword">int</span> search_cnt = <span class="number">1</span>&lt;&lt;<span class="number">10</span>;
    srand( (<span class="keyword">unsigned</span> <span class="keyword">int</span>)time(<span class="number">0</span>) );
    <span class="keyword">for</span>( i = <span class="number">0</span> ; i &lt; search_cnt ; i++ )
    {
        rbtree_find(&amp;t, (rand()%cnt)+<span class="number">1</span>);
    }

    <span class="keyword">long</span> time3 = clock();
    duration = (<span class="keyword">double</span>)(time3 - time2) / CLOCKS_PER_SEC;
    printf(<span class="string">"Searching %d nodes among %d spends %f seconds.\n"</span>, search_cnt, cnt, duration);

    <span class="keyword">const</span> <span class="keyword">int</span> delete_cnt = <span class="number">1</span>&lt;&lt;<span class="number">10</span>;
    <span class="keyword">int</span> nums[delete_cnt];
    <span class="keyword">int</span> num;
    <span class="comment">/* Let's hash! */</span>
    <span class="keyword">char</span>* mark = (<span class="keyword">char</span>*)calloc(cnt, <span class="keyword">sizeof</span>(<span class="keyword">char</span>));
    memset(mark, <span class="number">0</span>, cnt*<span class="keyword">sizeof</span>(<span class="keyword">char</span>));
    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; delete_cnt; i++)
    {
        <span class="keyword">for</span>(;;)
        {
            num = rand()%cnt;
            <span class="keyword">if</span> (mark[num] == <span class="number">0</span>)
            {
                mark[num] = <span class="number">1</span>;
                nums[i] = num;
                <span class="keyword">break</span>;
            }
        }
    }

    <span class="keyword">long</span> time4 = clock();
    duration = (<span class="keyword">double</span>)(time4 - time3) / CLOCKS_PER_SEC;
    printf(<span class="string">"Hash %d times spends %f seconds.\n"</span>, delete_cnt, duration);

    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; delete_cnt; i++)
    {
        rbtree_delete(&amp;t, &amp;n[nums[i]]);
    }

    <span class="keyword">long</span> time5 = clock();
    duration = (<span class="keyword">double</span>)(time5 - time4) / CLOCKS_PER_SEC;
    printf(<span class="string">"Deleting %d nodes among %d spends %f seconds.\n"</span>, delete_cnt, cnt, duration);
    free(mark);
    free(n);

    <span class="keyword">return</span> <span class="number">0</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 来看看结果：</p>
<pre><code>Inserting <span class="number">1048576</span> nodes costs <span class="number">48.00</span>MB and spends <span class="number">0.425416</span> seconds.
Searching <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.001140</span> seconds.
Hash <span class="number">1024</span> times spends <span class="number">0.000334</span> seconds.
Deleting <span class="number">1024</span> nodes among <span class="number">1048576</span> spends <span class="number">0.000783</span> seconds.
</code></pre><p>&nbsp;&nbsp;&nbsp; 删除比查找还快，耗时只有哈希查找的两倍多点，上百万的插入也耗时不足半秒，嗯我还挺满意的。<br>&nbsp;&nbsp;&nbsp; 写统计和遍历方法去了。</p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 不管看了多少遍有多滚瓜烂熟，数据结构或者算法的代码如果不亲自码一遍，我总是不敢说自己懂了。这次就来自己动手重写红黑树。<br>]]>
    
    </summary>
    
      <category term="C" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/tags/nginx/"/>
    
      <category term="rbtree" scheme="http://blog.bipedalbit.net/tags/rbtree/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/categories/nginx/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[C语言的static关键字]]></title>
    <link href="http://blog.bipedalbit.net/2015/8/11/C%E8%AF%AD%E8%A8%80%E7%9A%84static%E5%85%B3%E9%94%AE%E5%AD%97/"/>
    <id>http://blog.bipedalbit.net/2015/8/11/C语言的static关键字/</id>
    <published>2015-08-11T12:09:08.000Z</published>
    <updated>2015-09-15T13:23:44.179Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; static这个关键字可谓熟悉又陌生，要比较系统地理解它，更好地使用它，需要对它有更加深入的了解。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; C程序一直由下列部分组成：<br>&nbsp;&nbsp;&nbsp; 1）正文段——CPU执行的机器指令部分；一个程序只有一个副本；只读，防止程序由于意外事故而修改自身指令。<br>&nbsp;&nbsp;&nbsp; 2）初始化数据段（数据段）——在程序中所有赋了初值的全局变量，存放在这里。<br>&nbsp;&nbsp;&nbsp; 3）非初始化数据段（bss段）——在程序中没有初始化的全局变量；内核将此段初始化为0。<br>&nbsp;&nbsp;&nbsp; 4）栈——增长方向：自顶向下增长；自动变量以及每次函数调用时所需要保存的信息（返回地址、环境信息）。<br>&nbsp;&nbsp;&nbsp; 5）堆——动态存储分配的内存。</p>
<p><span style="color:#FF0000;"><span style="color:#000000;">1.</span>全局静态变量</span></p>
<p>&nbsp;&nbsp;&nbsp; 在全局变量之前加上关键字static，全局变量就被定义成为一个全局静态变量。<br>&nbsp;&nbsp;&nbsp; 1）内存中的位置：静态存储区。（静态存储区在整个程序运行期间都存在）<br>&nbsp;&nbsp;&nbsp; 2）初始化：未经初始化的全局静态变量会被程序自动初始化为0。（自动对象的值是任意的，除非他被显示初始化）<br>&nbsp;&nbsp;&nbsp; 3）作用域：全局静态变量在声明他的文件之外是不可见的。准确地讲从定义之处开始到文件结尾。</p>
<p>&nbsp;&nbsp;&nbsp; 定义全局静态变量的好处：<br>&nbsp;&nbsp;&nbsp; 1）不会被其他文件所访问，修改。<br>&nbsp;&nbsp;&nbsp; 2）其他文件中可以使用相同名字的变量，不会发生冲突。</p>
<p><span style="color:#FF0000;"><span style="color:#000000;">2.</span>局部静态变量</span></p>
<p>&nbsp;&nbsp;&nbsp; 在局部变量之前加上关键字static，局部变量就被定义成为一个局部静态变量。<br>&nbsp;&nbsp;&nbsp; 1）内存中的位置：静态存储区。<br>&nbsp;&nbsp;&nbsp; 2）初始化：未经初始化的全局静态变量会被程序自动初始化为0。（自动对象的值是任意的，除非他被显示初始化）<br>&nbsp;&nbsp;&nbsp; 3）作用域：作用域仍为局部作用域，当定义它的函数或者语句块结束的时候，作用域随之结束。</p>
<p>&nbsp;&nbsp;&nbsp;<span style="color:#FF0000;">注：当static用来修饰局部变量的时候，它就改变了局部变量的存储位置，从原来的栈中存放改为静态存储区。但是局部静态变量在离开作用域之后，并没有被销毁，而是仍然驻留在内存当中，直到程序结束，只不过我们不能再对他进行访问。</span><br>&nbsp;&nbsp;&nbsp; 当static用来修饰全局变量的时候，它就改变了全局变量的作用域（在声明他的文件之外是不可见的），但是没有改变它的存放位置，还是在静态存储区中。</p>
<p>3.<span style="color:#FF0000;">静态函数</span><br>&nbsp;&nbsp;&nbsp; 在函数的返回类型前加上关键字static，函数就被定义成为静态函数。<br>&nbsp;&nbsp;&nbsp; 函数的定义和声明默认情况下是extern的，但静态函数只是在声明他的文件当中可见，不能被其他文件所用。</p>
<p>&nbsp;&nbsp;&nbsp; 定义静态函数的好处：<br>&nbsp;&nbsp;&nbsp; 1）其他文件中可以定义相同名字的函数，不会发生冲突<br>&nbsp;&nbsp;&nbsp; 2）静态函数不能被其他文件所用。存储说明符auto，register，extern，static，对应两种存储期：自动存储期和静态存储期。auto和register对应自动存储期。具有自动存储期的变量在进入声明该变量的程序块时被建立，它在该程序块活动时存在，退出该程序块时撤销。</p>
<p>&nbsp;&nbsp;&nbsp; 关键字extern和static用来说明具有静态存储期的变量和函数。用static声明的局部变量具有静态存储持续期（static storage duration），或静态范围（static extent）。虽然他的值在函数调用之间保持有效，但是其名字的可视性仍限制在其局部域内。静态局部对象在程序执行到该对象的声明处时被首次初始化。</p>
<p>&nbsp;&nbsp;&nbsp; 由于static变量的以上特性，可实现一些特定功能。</p>
<p>&nbsp;&nbsp;&nbsp; 统计次数功能：<br>&nbsp;&nbsp;&nbsp; 声明函数的一个局部变量，并设为static类型，作为一个计数器，这样函数每次被调用的时候就可以进行计数。这是统计函数被调用次数的最好的办法，因为这个变量是和函数息息相关的，而函数可能在多个不同的地方被调用，所以从调用者的角度来统计比较困难。</p>
<p><span style="color:#FF0000;">&nbsp;&nbsp;&nbsp; 使用静态函数的好处：<br>&nbsp;&nbsp;&nbsp; 静态函数会被自动分配在一个一直使用的存储区，直到退出应用程序实例，避免了调用函数时压栈出栈，速度快很多。<br>&nbsp;&nbsp;&nbsp; 关键字“static”，译成中文就是“静态的”，所以内部函数又称静态函数。但此处“static”的含义不是指存储方式，而是指对函数的作用域仅局限于本文件。使用内部函数的好处是：不同的人编写不同的函数时，不用担心自己定义的函数，是否会与其它文件中的函数同名，因为同名也没有关系。</span></p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; static这个关键字可谓熟悉又陌生，要比较系统地理解它，更好地使用它，需要对它有更加深入的了解。<br>]]>
    
    </summary>
    
      <category term="C" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="static" scheme="http://blog.bipedalbit.net/tags/static/"/>
    
      <category term="内存分布" scheme="http://blog.bipedalbit.net/tags/%E5%86%85%E5%AD%98%E5%88%86%E5%B8%83/"/>
    
      <category term="C" scheme="http://blog.bipedalbit.net/categories/C/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[【三】nginx的数据结构(1)——ngx_int_t与ngx_rbtree_t]]></title>
    <link href="http://blog.bipedalbit.net/2015/8/6/%E3%80%90%E4%B8%89%E3%80%91nginx%E7%9A%84%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-1-%E2%80%94%E2%80%94ngx-int-t%E4%B8%8Engx-rbtree-t/"/>
    <id>http://blog.bipedalbit.net/2015/8/6/【三】nginx的数据结构-1-——ngx-int-t与ngx-rbtree-t/</id>
    <published>2015-08-05T23:18:00.000Z</published>
    <updated>2015-09-15T14:55:55.407Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 就来挑两个特别的数据结构ngx_int_t、ngx_rbtree_t作为研读nginx源码的开始吧。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; 面对<span style="font-family:Times New Roman;">./src/core</span><span style="font-family:宋体;">子目录中</span><span style="font-family:Times New Roman;">71</span><span style="font-family:宋体;">个源文件，有点无从下手。浏览包含主函数的</span><span style="font-family:Times New Roman;">nginx.c</span><span style="font-family:宋体;">文件，发现</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">使用了很多自行封装的数据结构，不弄清楚这是些什么样的数据结构就很难理解主函数中操作的意义。于是我们挑看起来基础的数据结构开始研究。组织</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">所有数据结构的是</span><span style="font-family:Times New Roman;">ngx_core.h</span><span style="font-family:宋体;">文件。它首先包含了</span><span style="font-family:Times New Roman;">ngx_config.h</span><span style="font-family:宋体;">，我们在</span><span style="font-family:Times New Roman;">ngx_config.h</span><span style="font-family:宋体;">中发现了三个类型定义。</span></p>
<h1 id="1-_ngx_int_t、ngx_uint_t、ngx_flag_t">1. ngx_int_t<span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">ngx_uint_t</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">ngx_flag_t</span></h1><p>&nbsp;&nbsp;&nbsp; nginx.c<span style="font-family:宋体;">中看到的第一个陌生数据类型是</span><span style="font-family:Times New Roman;">ngx_int_t</span><span style="font-family:宋体;">，在</span><span style="font-family:Times New Roman;">nginx_config.h</span><span style="font-family:宋体;">中找到了它的定义。</span></p>
<pre><code><span class="keyword">typedef</span> <span class="keyword">intptr_t</span>        <span class="keyword">ngx_int_t</span>;
<span class="keyword">typedef</span> <span class="keyword">uintptr_t</span>       <span class="keyword">ngx_uint_t</span>;
<span class="keyword">typedef</span> <span class="keyword">intptr_t</span>        <span class="keyword">ngx_flag_t</span>;
</code></pre><p>&nbsp;&nbsp;&nbsp; 顺藤摸瓜找到了三个数据类型的定义。本科<span style="font-family:Times New Roman;">c</span><span style="font-family:宋体;">入门教学中并没有对</span><span style="font-family:Times New Roman;">intptr_t/uintptr_t</span><span style="font-family:宋体;">的介绍，我在</span><span style="font-family:Times New Roman;">c</span><span style="font-family:宋体;">的</span><span style="font-family:Times New Roman;">stdint.h</span><span style="font-family:宋体;">头文件中发现了它们的定义。</span></p>
<pre><code><span class="comment">/* Types for `void *' pointers.  */</span>
<span class="preprocessor">#<span class="keyword">if</span> __WORDSIZE == <span class="number">64</span></span>
<span class="preprocessor"># <span class="keyword">ifndef</span> __intptr_t_defined</span>
<span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">int</span>               <span class="keyword">intptr_t</span>;
<span class="preprocessor">#  <span class="keyword">define</span> __intptr_t_defined</span>
<span class="preprocessor"># <span class="keyword">endif</span></span>
<span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">int</span>    <span class="keyword">uintptr_t</span>;
<span class="preprocessor">#<span class="keyword">else</span></span>
<span class="preprocessor"># <span class="keyword">ifndef</span> __intptr_t_defined</span>
<span class="keyword">typedef</span> <span class="keyword">int</span>                    <span class="keyword">intptr_t</span>;
<span class="preprocessor">#  <span class="keyword">define</span> __intptr_t_defined</span>
<span class="preprocessor"># <span class="keyword">endif</span></span>
<span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">int</span>        <span class="keyword">uintptr_t</span>;
<span class="preprocessor">#<span class="keyword">endif</span></span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 首先注释说这两种类型是<span style="font-family:Times New Roman;">“void&nbsp;*”</span><span style="font-family:宋体;">的指针类型，尽管字面上看，</span><span style="font-family:Times New Roman;">intptr_t</span><span style="font-family:宋体;">和</span><span style="font-family:Times New Roman;">uintptr_t</span><span style="font-family:宋体;">确实是整型指针类型和无符号整型指针类型，但是让人摸不着头脑，为什么要使用整型作为整型的指针类型呢？先放一放，看后面的宏，机器是</span><span style="font-family:Times New Roman;">64</span><span style="font-family:宋体;">位字长则</span><span style="font-family:Times New Roman;">intptr_t</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">long&nbsp;int</span><span style="font-family:宋体;">，</span><span style="font-family:Times New Roman;">uintptr_t</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">unsigned&nbsp;long&nbsp;int</span><span style="font-family:宋体;">，正好我机器上是</span><span style="font-family:Times New Roman;">64</span><span style="font-family:宋体;">位编译器，</span><span style="font-family:Times New Roman;">sizeof()</span><span style="font-family:宋体;">了一下，是</span><span style="font-family:Times New Roman;">8</span><span style="font-family:宋体;">个字节</span><span style="font-family:Times New Roman;">64</span><span style="font-family:宋体;">位，小于</span><span style="font-family:Times New Roman;">64</span><span style="font-family:宋体;">位字长的</span><span style="font-family:Times New Roman;">intptr_t</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">int</span><span style="font-family:宋体;">，</span><span style="font-family:Times New Roman;">uintptr_t</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">unsigned&nbsp;int</span><span style="font-family:宋体;">，查表得知</span><span style="font-family:Times New Roman;">32</span><span style="font-family:宋体;">位编译器下</span><span style="font-family:Times New Roman;">int</span><span style="font-family:宋体;">和</span><span style="font-family:Times New Roman;">unsigned</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">4</span><span style="font-family:宋体;">个字节，</span><span style="font-family:Times New Roman;">16</span><span style="font-family:宋体;">位编译器下为</span><span style="font-family:Times New Roman;">2</span><span style="font-family:宋体;">个字节。那么</span><span style="font-family:Times New Roman;">intptr_t/uintptr_t</span><span style="font-family:宋体;">应该是会随着平台字长变化而发生对应变化的整型类型。经过了解，发现《深入分析</span><span style="font-family:Times New Roman;">Linux</span><span style="font-family:宋体;">内核源码》中对此的解释是，系统内核在操作内存时，将内存当做一个大数组，而指针就是数组索引</span><span style="font-family:Times New Roman;">/</span><span style="font-family:宋体;">下标，内核程序员使用这种特殊的整型来接受内存地址值、操作内存相比使用指针更加直观，不容易犯错。看起来，</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">中，只是单纯的想要使用一些平台相关的</span><span style="font-family:Times New Roman;">int</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">unsigned&nbsp;int</span><span style="font-family:宋体;">类型变量而已。</span></p>
<h1 id="2-_ngx_rbtree_t">2. ngx_rbtree_t</h1><h2 id="2-1_什么是红黑树">2.1 <span style="font-family:宋体;">什么是红黑树</span></h2><p>&nbsp;&nbsp;&nbsp; 作为一个曾经常年在<span style="font-family:Times New Roman;">ACM</span><span style="font-family:宋体;">比赛里划水的退役队员，对红黑树这样的有名数据结构还是比较敏感的。红黑树是一种特殊约束形式下的平衡二叉查找树实现。学过数据结构课的同学应该知道，课本上的最早的自平衡二叉树</span><span style="font-family:Times New Roman;">AVL</span><span style="font-family:宋体;">树严格的要求子树的高度差不超过</span><span style="font-family:Times New Roman;">2</span><span style="font-family:宋体;">，以获得根结点到所有叶结点距离基本相同（平衡）的特性。</span></p>
<p>&nbsp;&nbsp;&nbsp; 红黑树不追求严格的平衡，而是通过<span style="font-family:Times New Roman;">5</span><span style="font-family:宋体;">个约束实现基本平衡：</span><br>&nbsp;&nbsp;&nbsp; ①结点是红色或黑色；<br>&nbsp;&nbsp;&nbsp; ②根是黑色；<br>&nbsp;&nbsp;&nbsp; ③叶结点是黑色；<br>&nbsp;&nbsp;&nbsp; ④红色结点的子结点都是黑色；<br>&nbsp;&nbsp;&nbsp; ⑤任一结点到其叶结点的简单路径中黑色结点数相同。</p>
<p>&nbsp;&nbsp;&nbsp; AVL树根到叶结点最长距离与最短距离的比不超过2。红黑树的约束也保证了这一特性（最长路径是红黑相间，最短路径是全黑，这种情况下最长路径刚好是最短路径的2倍长）。</p>
<p>&nbsp;&nbsp;&nbsp; 既然是平衡二叉查找树的一种实现，那么红黑树自然是内部有序的，同时跟AVL树一样支持O(log2n)时间复杂度的查找、插入和删除。</p>
<p>&nbsp;&nbsp;&nbsp; 相比AVL树，红黑可以保证在每次插入或删除操作之后的重平衡过程中，全树拓扑结构的更新仅涉及常数个结点。尽管最坏情况下需对O(log2n)个结点重染色，但就分摊意义（平均效率）而言，仅为O(1)个。但是因为没有严格约束树的平衡特性，红黑树的左右子树高度差比AVL树要大。</p>
<h2 id="2-2_ngx_rbtree-h">2.2 ngx_rbtree.h</h2><p>&nbsp;&nbsp;&nbsp; 机会难得，我们就把nginx的源码作为素材来深入了解一下红黑树的实现。首先是结点的结构：</p>
<pre><code><span class="keyword">typedef</span> <span class="keyword">ngx_uint_t</span>  <span class="keyword">ngx_rbtree_key_t</span>;
<span class="keyword">typedef</span> <span class="keyword">ngx_int_t</span>   <span class="keyword">ngx_rbtree_key_int_t</span>;


<span class="keyword">typedef</span> <span class="keyword">struct</span> ngx_rbtree_node_s  <span class="keyword">ngx_rbtree_node_t</span>;

<span class="keyword">struct</span> ngx_rbtree_node_s {
    <span class="keyword">ngx_rbtree_key_t</span>       key;<span class="comment">//平台相关的无符号整型关键字</span>
    <span class="keyword">ngx_rbtree_node_t</span>     *left;<span class="comment">//左子结点指针</span>
    <span class="keyword">ngx_rbtree_node_t</span>     *right;<span class="comment">//&lt;span style=&amp;quot;font-family:宋体;&amp;quot;&gt;右&lt;/span&gt;子结点指针</span>
    <span class="keyword">ngx_rbtree_node_t</span>     *parent;<span class="comment">//父结点指针</span>
    u_char                 color;<span class="comment">//结点颜色</span>
    u_char                 data;<span class="comment">//结点数据</span>
};
</code></pre><p>&nbsp;&nbsp;&nbsp; 然后是红黑树的结构定义：</p>
<pre><code><span class="keyword">typedef</span> <span class="keyword">struct</span> ngx_rbtree_s  <span class="keyword">ngx_rbtree_t</span>;    <span class="comment">//“_s”是结构体“_t”是类型</span>
<span class="comment">//下面是一个函数指针变量类型的定义，是红黑树插入函数的指针</span>
<span class="comment">//参数有树根结点、插入结点和哨兵结点的指针</span>
<span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*ngx_rbtree_insert_pt)</span> <span class="params">(ngx_rbtree_node_t *root,
    ngx_rbtree_node_t *node, ngx_rbtree_node_t *sentinel)</span></span>;

<span class="keyword">struct</span> ngx_rbtree_s {
    <span class="keyword">ngx_rbtree_node_t</span>     *root;    <span class="comment">//根节点指针</span>
    <span class="keyword">ngx_rbtree_node_t</span>     *sentinel;    <span class="comment">//哨兵结点指针</span>
    ngx_rbtree_insert_pt   insert;    <span class="comment">//插入函数指针</span>
};
</code></pre><p>&nbsp;&nbsp;&nbsp; 将函数指针变量作为结构体成员变量以达成可以把结构体当做类来使用（既有成员变量又有成员方法）的效果，这种手法在<span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">的源码中相当普遍。关于函数，</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">还有一种更神奇的手段</span><span style="font-family:Times New Roman;">——</span><span style="font-family:宋体;">宏：</span></p>
<pre><code><span class="comment">#define ngx_rbtree_init(tree, s, i)                                         \</span>
    ngx_rbtree_sentinel_init(s);                                               <span class="string">\</span>
    <span class="function"><span class="params">(tree)</span>-&gt;</span>root = s;                                                                  <span class="string">\</span>
    <span class="function"><span class="params">(tree)</span>-&gt;</span>sentinel = s;                                                            <span class="string">\</span>
    <span class="function"><span class="params">(tree)</span>-&gt;</span>insert = i<span class="pi">//这里insert函数指针的赋值实现了多态</span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 借助宏来达成内联函数的效果（函数实现如果比较简单，就干脆把实现过程整个搬到类中），令人费解的是，<span style="font-family:Times New Roman;">C</span><span style="font-family:宋体;">不是没有内联关键字，甚至同一个头文件中就有一个内联函数的定义。研究内联函数之前，下面还有几个宏要看一看：</span></p>
<pre><code><span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbt_red</span><span class="params">(node)</span></span>               ((node)-&gt;<span class="attribute">color</span> = <span class="number">1</span>)
<span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbt_black</span><span class="params">(node)</span></span>             ((node)-&gt;<span class="attribute">color</span> = <span class="number">0</span>)
<span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbt_is_red</span><span class="params">(node)</span></span>            ((node)-&gt;<span class="attribute">color</span>)
<span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbt_is_black</span><span class="params">(node)</span></span>          (!<span class="function"><span class="title">ngx_rbt_is_red</span><span class="params">(node)</span></span>)
<span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbt_copy_color</span><span class="params">(n1, n2)</span></span>      (n1-&gt;<span class="attribute">color</span> = n2-&gt;<span class="attribute">color</span>)

<span class="comment">/* a sentinel must be black */</span>
<span class="hexcolor">#def</span>ine <span class="function"><span class="title">ngx_rbtree_sentinel_init</span><span class="params">(node)</span></span>  <span class="function"><span class="title">ngx_rbt_black</span><span class="params">(node)</span></span>
</code></pre><p>&nbsp;&nbsp;&nbsp; nginx<span style="font-family:宋体;">源码中的变量都很容易看懂以至于我们不怎么需要查资料或找注释。</span><span style="font-family:Times New Roman;">color</span><span style="font-family:宋体;">置</span><span style="font-family:Times New Roman;">1</span><span style="font-family:宋体;">染红置</span><span style="font-family:Times New Roman;">0</span><span style="font-family:宋体;">染黑，</span><span style="font-family:Times New Roman;">color</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">1</span><span style="font-family:宋体;">则结点为红色，不为红色的则为黑色，复制结点颜色即复制</span><span style="font-family:Times New Roman;">color</span><span style="font-family:宋体;">值，哨兵结点一定要染成黑色。</span></p>
<pre><code><span class="keyword">static</span> ngx_inline ngx_rbtree_node_t *
ngx_rbtree_min(ngx_rbtree_node_t *node, ngx_rbtree_node_t *sentinel)
{
    <span class="keyword">while</span> (node-&gt;left != sentinel) {
        node = node-&gt;left;
    }
    <span class="keyword">return</span> node;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; ngx_inline是一个宏，实际值就是关键字inline。这个内联函数非常好懂，目的看起来是寻找以任意结点为根结点的子树中结点值最小的结点。实现方法是找到红黑树子树最边缘的左子结点。那么我们有理由猜测，哨兵结点是空结点或边缘标识。</p>
<h2 id="2-3_红黑树的结点插入"><span style="font-family:宋体;">2.3 红黑树的结点插入</span></h2><p>&nbsp;&nbsp;&nbsp; 接下来我们来深入<span style="font-family:Times New Roman;">ngx_rbtree.c</span><span style="font-family:宋体;">看看</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">如何实现几个关键的红黑树方法。</span></p>
<pre><code><span class="literal">void</span>
ngx_rbtree_insert(ngx_rbtree_t *tree, ngx_rbtree_node_t *node)
{
    <span class="comment">//根结点指针的指针，或者根结点指针数组，会有多个根结点吗，令人费解</span>
    <span class="comment">//临时结点指针</span>
    <span class="comment">//哨兵结点指针，推测哨兵在每次查询时可能都不一样，也许指待插位置</span>
    <span class="comment">//变量不分行，我写注释都很不方便</span>
    ngx_rbtree_node_t  **root, *temp, *sentinel;

    <span class="comment">/* a binary tree insert */</span>

    root = (ngx_rbtree_node_t **) <span class="subst">&amp;</span>tree<span class="subst">-&gt;</span>root;<span class="comment">//树根指针的指针赋给了root</span>
    sentinel = tree<span class="subst">-&gt;</span>sentinel;<span class="comment">//哨兵指针赋给了哨兵指针</span>

    <span class="keyword">if</span> (*root == sentinel) {<span class="comment">//特判，如果根是哨兵，即树是空的</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="built_in">NULL</span>;<span class="comment">//新插入的结点变成了根</span>
        node<span class="subst">-&gt;</span>left = sentinel;<span class="comment">//新结点的左子结点是哨兵</span>
        node<span class="subst">-&gt;</span>right = sentinel;<span class="comment">//新结点的右子结点也是哨兵</span>
        ngx_rbt_black(node);<span class="comment">//新根染黑</span>
        *root = node;<span class="comment">//确认新结点为新根</span>

        <span class="keyword">return</span>;<span class="comment">//插入结束</span>
    }

    <span class="comment">//树初始化时给了insert指针一个函数地址</span>
    <span class="comment">//查看前面的宏ngx_rbtree_init(tree, s, i)</span>
    <span class="comment">//发现只是把指定结点染黑，同时赋为根和哨兵，给insert指针指定一个函数</span>
    <span class="comment">//ngx_rbtree.c中有两个参数表符合的可选函数：插入值、插入计时器值</span>
    <span class="comment">//稍后来看两种插入分别如何实现又有什么区别</span>
    tree<span class="subst">-&gt;</span>insert(*root, node, sentinel);

    <span class="comment">/* re-balance tree */</span>
    <span class="comment">//如果新结点不是根且其父结点是红的，循环</span>
    <span class="keyword">while</span> (node != *root <span class="subst">&amp;&amp;</span> ngx_rbt_is_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span>)) {
        <span class="comment">//如果父结点是左子结点，获得父结点的右兄弟</span>
        <span class="keyword">if</span> (node<span class="subst">-&gt;</span><span class="keyword">parent</span> == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left) {
            temp = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;
            <span class="comment">//如果父结点的右兄弟是红的</span>
            <span class="keyword">if</span> (ngx_rbt_is_red(temp)) {
                ngx_rbt_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//父结点染黑</span>
                ngx_rbt_black(temp);<span class="comment">//父结点的右兄弟染黑</span>
                ngx_rbt_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//父结点的父结点染红</span>
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//父结点的父结点成为当前结点</span>

            } <span class="keyword">else</span> {<span class="comment">//如果父结点的右兄弟是黑的</span>
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right) {<span class="comment">//如果新结点是右子结点</span>
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//父结点成为新node</span>
                    ngx_rbtree_left_rotate(root, sentinel, node);<span class="comment">//node左旋</span>

                }

                ngx_rbt_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//node的父结点染黑</span>
                <span class="comment">//node的父结点的父结点染红</span>
                ngx_rbt_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbtree_right_rotate(root, sentinel, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//node的父结点的父结点右旋</span>
            }

        } <span class="keyword">else</span> {<span class="comment">//如果父结点是右子结点，获得父结点的左兄弟</span>
            temp = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            <span class="comment">//如果父结点的左兄弟是红的</span>
            <span class="keyword">if</span> (ngx_rbt_is_red(temp)) {
                ngx_rbt_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//父结点染黑</span>
                ngx_rbt_black(temp);<span class="comment">//父结点的左兄弟染黑</span>
                ngx_rbt_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//父结点的父结点染红</span>
                node = node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>;

            } <span class="keyword">else</span> {<span class="comment">//如果父结点的左兄弟是黑的</span>
                <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left) {<span class="comment">//如果新结点是左子结点</span>
                    node = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//父结点成为当前结点</span>
                    ngx_rbtree_right_rotate(root, sentinel, node);
                    <span class="comment">//当前结点右旋</span>
                }

                ngx_rbt_black(node<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//当前结点染黑</span>
                <span class="comment">//当前结点父结点的父结点染红</span>
                ngx_rbt_red(node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbtree_left_rotate(root, sentinel, node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//当前结点的父结点的父结点左旋</span>
            }
        }
    }

    ngx_rbt_black(*root);<span class="comment">//根结点染黑</span>
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 然后是对应ngx_rbtree_insert_pt指针的基础的结点插入函数：</p>
<pre><code><span class="literal">void</span>
ngx_rbtree_insert_value(ngx_rbtree_node_t *temp, ngx_rbtree_node_t *node,
    ngx_rbtree_node_t *sentinel)
{
    ngx_rbtree_node_t  **p;<span class="comment">//虽然无关紧要，但两层指针令人费解</span>

    f<span class="subst">or</span> ( ;; ) {<span class="comment">//无条件循环或者说死循环，等同于while(1)但节省了一个字符</span>

        p = (node<span class="subst">-&gt;</span>key &lt; temp<span class="subst">-&gt;</span>key) ? <span class="subst">&amp;</span>temp<span class="subst">-&gt;</span>left : <span class="subst">&amp;</span>temp<span class="subst">-&gt;</span>right;

        <span class="keyword">if</span> (*p == sentinel) {<span class="comment">//在二叉树中查找新结点合适的叶结点位置</span>
            break;
        }

        temp = *p;
    }
    <span class="comment">//令新结点占据合适的哨兵位置成为新的叶结点，染红，产生新哨兵</span>
    *p = node;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = temp;
    node<span class="subst">-&gt;</span>left = sentinel;
    node<span class="subst">-&gt;</span>right = sentinel;
    ngx_rbt_red(node);
}
</code></pre><p>&nbsp;&nbsp;&nbsp; ngx_rbtree_insert_timer_value<span style="font-family:宋体;">函数跟</span><span style="font-family:Times New Roman;">ngx_rbtree_insert_value</span><span style="font-family:宋体;">函数唯一区别就是判断大小时，采用了两个值相减，避免溢出。</span></p>
<p>&nbsp;&nbsp;&nbsp; 以上是插入结点涉及的函数，老实说我不太喜欢这么长的函数实现，换我自己写肯定分块了。分支操作太多，看代码逻辑已经乱了，我们需要画几个图。首先，如果树为空：</p>
<p><img src="http://img.blog.csdn.net/20150806074055339" alt=""><br></p>
<p>&nbsp;&nbsp;&nbsp; 如果树中只有一个根结点：</p>
<p><img src="http://img.blog.csdn.net/20150806074132691" alt=""></p>
<p>&nbsp;&nbsp;&nbsp; 如果<span style="font-family:Times New Roman;">C&gt;A</span><span style="font-family:宋体;">：</span></p>
<p><img src="http://img.blog.csdn.net/20150806074207378" alt=""></p>
<p>&nbsp;&nbsp;&nbsp; 如果<span style="font-family:Times New Roman;">C<b<a< span=""><span style="font-family:宋体;">，</span><span style="font-family:Times New Roman;">C</span><span style="font-family:宋体;">染红，</span><span style="font-family:Times New Roman;">B</span><span style="font-family:宋体;">染黑</span><span style="font-family:Times New Roman;">A</span><span style="font-family:宋体;">染红，</span><span style="font-family:Times New Roman;">A</span><span style="font-family:宋体;">右旋。右旋函数如下：</span></b<a<></span></p>
<pre><code>static ngx_inline <span class="literal">void</span>
ngx_rbtree_right_rotate(ngx_rbtree_node_t **root, ngx_rbtree_node_t *sentinel,
    ngx_rbtree_node_t *node)
{
    ngx_rbtree_node_t  *temp;

    temp = node<span class="subst">-&gt;</span>left;
    node<span class="subst">-&gt;</span>left = temp<span class="subst">-&gt;</span>right;<span class="comment">//左子结点指向原左子结点的右结点</span>

    <span class="keyword">if</span> (temp<span class="subst">-&gt;</span>right != sentinel) {<span class="comment">//如果左子结点的右结点不为哨兵</span>
        temp<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span><span class="keyword">parent</span> = node;<span class="comment">//左子结点的右子结点挂在右旋结点上</span>
    }

    temp<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//左子结点挂在右旋结点的父结点上</span>

    <span class="keyword">if</span> (node == *root) {<span class="comment">//如果右旋结点为根节点</span>
        *root = temp;<span class="comment">//根节点赋为左子结点</span>

    } <span class="keyword">else</span> <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right) {<span class="comment">//如果右旋结点为右子结点</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = temp;<span class="comment">//左子结点挂父结点右边</span>

    } <span class="keyword">else</span> {<span class="comment">//否则左子结点挂父结点左边</span>
        node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = temp;
    }

    temp<span class="subst">-&gt;</span>right = node;<span class="comment">//右旋结点挂左子结点右边</span>
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = temp;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 显然<span style="font-family:Times New Roman;">B</span><span style="font-family:宋体;">将成为新的根，左</span><span style="font-family:Times New Roman;">C</span><span style="font-family:宋体;">右</span><span style="font-family:Times New Roman;">A</span><span style="font-family:宋体;">：</span></p>
<p><img src="http://img.blog.csdn.net/20150806074443292" alt=""></p>
<p>&nbsp;&nbsp;&nbsp; 如果<span style="font-family:Times New Roman;">B<c<a< span=""><span style="font-family:宋体;">，会先做一次左旋再做一次右旋，其实除开染色过程，我觉得这跟</span><span style="font-family:Times New Roman;">AVL</span><span style="font-family:宋体;">树的插入过程没有什么区别：</span></c<a<></span></p>
<p><img src="http://img.blog.csdn.net/20150806074520668" alt=""></p>
<p>&nbsp;&nbsp;&nbsp; 其他的插入情景要么与以上几个对称，要么发生在树的其他子树中，实际过程完全一样。LL<span style="font-family:宋体;">型右旋，</span><span style="font-family:Times New Roman;">RR</span><span style="font-family:宋体;">型左旋，</span><span style="font-family:Times New Roman;">LR</span><span style="font-family:宋体;">型先右旋后左旋，</span><span style="font-family:Times New Roman;">RL</span><span style="font-family:宋体;">型先左旋后右旋。</span>与<span style="font-family:Times New Roman;">AVL</span><span style="font-family:宋体;">树不同的是，插入结点时红黑树左旋或右旋的判定条件明确为附近一两个结点的颜色，其他过程没有任何区别。</span></p>
<h2 id="2-4_红黑树的结点删除">2.4 红黑树的结点删除</h2><p>&nbsp;&nbsp;&nbsp; 据说红黑树和<span style="font-family:Times New Roman;">AVL</span><span style="font-family:宋体;">树的区别主要体现在删除节点时，我们就来看一看。</span>我刚说什么来着，删除结点的函数体更长了，足足<span style="font-family:Times New Roman;">165</span><span style="font-family:宋体;">行，我决定分段研究，</span>先看第一部分：</p>
<pre><code><span class="keyword">if</span> (node<span class="subst">-&gt;</span>left == sentinel) {<span class="comment">//如果左子结点是哨兵或左右子结点都是哨兵</span>
    temp = node<span class="subst">-&gt;</span>right;<span class="comment">//获得右子结点，后面让它接替node位置</span>
    subst = node;<span class="comment">//node赋给subst</span>

} <span class="keyword">else</span> <span class="keyword">if</span> (node<span class="subst">-&gt;</span>right == sentinel) {<span class="comment">//如果右子结点是哨兵</span>
    temp = node<span class="subst">-&gt;</span>left;<span class="comment">//获得左子结点，后面让它接替node位置</span>
    subst = node;<span class="comment">//node赋给subst</span>

} <span class="keyword">else</span> {<span class="comment">//如果左右子结点都不是哨兵</span>
    subst = ngx_rbtree_min(node<span class="subst">-&gt;</span>right, sentinel);<span class="comment">//获得右子树中最小的结点</span>

    <span class="keyword">if</span> (subst<span class="subst">-&gt;</span>left != sentinel) {<span class="comment">//如果右子树的最小结点的左子结点不是哨兵</span>
        temp = subst<span class="subst">-&gt;</span>left;<span class="comment">//获得右子树的最小结点的左子结点</span>
    } <span class="keyword">else</span> {<span class="comment">//否则获得右子树最小结点的右子结点</span>
        temp = subst<span class="subst">-&gt;</span>right;
    }<span class="comment">//看起来subst将被从原位置删掉然后接替node的位置</span>
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 下面我们来看看<span style="font-family:Times New Roman;">temp</span><span style="font-family:宋体;">和</span><span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">要干什么用：</span></p>
<pre><code><span class="keyword">if</span> (subst == *root) {<span class="comment">//如果subst是根</span>
    *root = temp;<span class="comment">//temp接替根</span>
    ngx_rbt_black(temp);<span class="comment">//染黑temp</span>

    <span class="comment">/* DEBUG stuff */</span>
    node<span class="subst">-&gt;</span>left = <span class="built_in">NULL</span>;<span class="comment">//清空了待删结点</span>
    node<span class="subst">-&gt;</span>right = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="built_in">NULL</span>;
    node<span class="subst">-&gt;</span>key = <span class="number">0</span>;

    <span class="keyword">return</span>;
}

red = ngx_rbt_is_red(subst);<span class="comment">//获得subst是否是红色</span>

<span class="keyword">if</span> (subst == subst<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left) {<span class="comment">//如果subst是左子结点</span>
    subst<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = temp;<span class="comment">//把接替结点挂到subst位置</span>

} <span class="keyword">else</span> {<span class="comment">//如果subst是右子结点</span>
    subst<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = temp;<span class="comment">//把接替结点挂到subst位置</span>
}
&lt;/pre&gt;

<span class="subst">&amp;</span>nbsp;<span class="subst">&amp;</span>nbsp;<span class="subst">&amp;</span>nbsp; 下一段：

&lt;pre name=<span class="string">"code"</span> class=<span class="string">"cpp"</span>&gt;<span class="keyword">if</span> (subst == node) {<span class="comment">//如果subst是待删结点</span>
    temp<span class="subst">-&gt;</span><span class="keyword">parent</span> = subst<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//接替结点直接接替，删除完成</span>

} <span class="keyword">else</span> {<span class="comment">//如果subst不是待删结点</span>
     <span class="keyword">if</span> (subst<span class="subst">-&gt;</span><span class="keyword">parent</span> == node) {<span class="comment">//如果subst的父结点就是待删结点</span>
        temp<span class="subst">-&gt;</span><span class="keyword">parent</span> = subst;<span class="comment">//接替结点挂在subst上</span>
     } <span class="keyword">else</span> {<span class="comment">//如果待删结点比subst的父结点更高</span>
        temp<span class="subst">-&gt;</span><span class="keyword">parent</span> = subst<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//把接替结点挂在subst的父结点上</span>
    }
    <span class="comment">//subst接替待删结点node的位置，复制待删结点跟周围结点的关系</span>
    subst<span class="subst">-&gt;</span>left = node<span class="subst">-&gt;</span>left;
    subst<span class="subst">-&gt;</span>right = node<span class="subst">-&gt;</span>right;
    subst<span class="subst">-&gt;</span><span class="keyword">parent</span> = node<span class="subst">-&gt;</span><span class="keyword">parent</span>;
    ngx_rbt_copy_col<span class="subst">or</span>(subst, node);<span class="comment">//复制颜色</span>

    <span class="keyword">if</span> (node == *root) {<span class="comment">//如果待删结点是根</span>
        *root = subst;<span class="comment">//subst接替根</span>
    } <span class="keyword">else</span> {<span class="comment">//如果待删结点不是根，subst接替它</span>
        <span class="keyword">if</span> (node == node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left) {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left = subst;
        } <span class="keyword">else</span> {
            node<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right = subst;
        }
    }

    <span class="keyword">if</span> (subst<span class="subst">-&gt;</span>left != sentinel) {<span class="comment">//如果subst左子结点不是哨兵</span>
        subst<span class="subst">-&gt;</span>left<span class="subst">-&gt;</span><span class="keyword">parent</span> = subst;<span class="comment">//subst的左子结点放弃node，挂上来</span>
    }

    <span class="keyword">if</span> (subst<span class="subst">-&gt;</span>right != sentinel) {<span class="comment">//如果subst右子结点不是哨兵</span>
        subst<span class="subst">-&gt;</span>right<span class="subst">-&gt;</span><span class="keyword">parent</span> = subst;<span class="comment">//subst右子结点放弃node，挂上来</span>
    }
}
<span class="comment">//清空待删结点node</span>
<span class="comment">/* DEBUG stuff */</span>
node<span class="subst">-&gt;</span>left = <span class="built_in">NULL</span>;
node<span class="subst">-&gt;</span>right = <span class="built_in">NULL</span>;
node<span class="subst">-&gt;</span><span class="keyword">parent</span> = <span class="built_in">NULL</span>;
node<span class="subst">-&gt;</span>key = <span class="number">0</span>;
<span class="comment">//如果subst是红色，红黑树约束依然被遵守，删除工作就可以结束了</span>
<span class="keyword">if</span> (red) {
    <span class="keyword">return</span>;
}
</code></pre><p>&nbsp;&nbsp;&nbsp; 看起来结点的删除过程已经顺利完成了，但是如果<span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">是黑色，我们需要修复红黑树的约束。下面这一段代码的主角是接替</span><span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">位置的</span><span style="font-family:Times New Roman;">temp</span><span style="font-family:宋体;">结点：</span></p>
<pre><code><span class="comment">//当subst的接替结点不是根且为黑色，循环</span>
<span class="keyword">while</span> (temp != *root <span class="subst">&amp;&amp;</span> ngx_rbt_is_black(temp)) {
        <span class="keyword">if</span> (temp == temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left) {<span class="comment">//如果temp是左子结点</span>
            w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;<span class="comment">//获得其右兄弟</span>

            <span class="keyword">if</span> (ngx_rbt_is_red(w)) {<span class="comment">//如果temp的右兄弟是红色</span>
                ngx_rbt_black(w);<span class="comment">//染黑temp的右兄弟</span>
                ngx_rbt_red(temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//染红temp的父结点</span>
                <span class="comment">//temp的父结点左旋</span>
                ngx_rbtree_left_rotate(root, sentinel, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;<span class="comment">//获得temp的新右兄弟</span>
            }
            <span class="comment">//如果temp右兄弟的左右子结点都是黑的</span>
            <span class="keyword">if</span> (ngx_rbt_is_black(w<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> ngx_rbt_is_black(w<span class="subst">-&gt;</span>right)) {
                ngx_rbt_red(w);<span class="comment">//染红temp的右兄弟</span>
                temp = temp<span class="subst">-&gt;</span><span class="keyword">parent</span>;<span class="comment">//获得temp的父结点为新temp</span>

            } <span class="keyword">else</span> {<span class="comment">//如果temp右兄弟的子结点不全为黑</span>
                <span class="keyword">if</span> (ngx_rbt_is_black(w<span class="subst">-&gt;</span>right)) {<span class="comment">//如果其右子结点是黑色</span>
                    ngx_rbt_black(w<span class="subst">-&gt;</span>left);<span class="comment">//染黑左子结点</span>
                    ngx_rbt_red(w);<span class="comment">//染红temp的右兄弟</span>
                    ngx_rbtree_right_rotate(root, sentinel, w);<span class="comment">//右兄弟右旋</span>
                    w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>right;<span class="comment">//获得temp的新右兄弟</span>
                }
                <span class="comment">//temp右兄弟复制temp父结点颜色</span>
                ngx_rbt_copy_col<span class="subst">or</span>(w, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbt_black(temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);<span class="comment">//染黑temp父结点</span>
                ngx_rbt_black(w<span class="subst">-&gt;</span>right);<span class="comment">//染黑temp右兄弟的右子结点</span>
                <span class="comment">//temp父结点左旋</span>
                ngx_rbtree_left_rotate(root, sentinel, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                temp = *root;<span class="comment">//获得根</span>
            }

        } <span class="keyword">else</span> {<span class="comment">//如果temp是右子结点，做对称的事</span>
            w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;

            <span class="keyword">if</span> (ngx_rbt_is_red(w)) {
                ngx_rbt_black(w);
                ngx_rbt_red(temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbtree_right_rotate(root, sentinel, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
            }

            <span class="keyword">if</span> (ngx_rbt_is_black(w<span class="subst">-&gt;</span>left) <span class="subst">&amp;&amp;</span> ngx_rbt_is_black(w<span class="subst">-&gt;</span>right)) {
                ngx_rbt_red(w);
                temp = temp<span class="subst">-&gt;</span><span class="keyword">parent</span>;

            } <span class="keyword">else</span> {
                <span class="keyword">if</span> (ngx_rbt_is_black(w<span class="subst">-&gt;</span>left)) {
                    ngx_rbt_black(w<span class="subst">-&gt;</span>right);
                    ngx_rbt_red(w);
                    ngx_rbtree_left_rotate(root, sentinel, w);
                    w = temp<span class="subst">-&gt;</span><span class="keyword">parent</span><span class="subst">-&gt;</span>left;
                }

                ngx_rbt_copy_col<span class="subst">or</span>(w, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbt_black(temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                ngx_rbt_black(w<span class="subst">-&gt;</span>left);
                ngx_rbtree_right_rotate(root, sentinel, temp<span class="subst">-&gt;</span><span class="keyword">parent</span>);
                temp = *root;
            }
        }
    }

ngx_rbt_black(temp);<span class="comment">//染黑当前temp</span>
</code></pre><p>&nbsp;&nbsp;&nbsp; 跟插入结点时一样乱，我们梳理一下。</p>
<p>&nbsp;&nbsp;&nbsp; 首先忽略红黑树的约束进行删除：<br>&nbsp;&nbsp;&nbsp; ①如果删除的是一个叶结点，即没有后继或后继全为哨兵的结点，直接删除即可；<br>&nbsp;&nbsp;&nbsp; ②如果只有一个后继，让其替换待删除结点即可；<br>&nbsp;&nbsp;&nbsp; ③如果有两个后继，需要从树的边缘选择一个结点，有两种等价的选择，待删结点左子树的最大结点和右子树的最小结点，<span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">选择的是后者，以这个结点的键与值（</span><span style="font-family:Times New Roman;">key</span><span style="font-family:宋体;">与</span><span style="font-family:Times New Roman;">value/data</span><span style="font-family:宋体;">）替换待删结点的键与值，然后删除这个替身。</span></p>
<p>&nbsp;&nbsp;&nbsp; 不论是①、②情景中的待删结点还是③情景中替身，在源码中都是subst。下面要围绕着它来进行讨论。</p>
<p>&nbsp;&nbsp;&nbsp; 以上是不考虑红黑树平衡性的纯拓扑结构变动。下面要考虑是否调整树的拓扑结构使树重新平衡，是否调整结点的颜色使树重新符合红黑树的约束条件。我们知道红黑树有一条关键约束是任意结点到其子树中叶结点的简单路径中黑色结点数相同。那么如果<span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">是一个红色结点，我们不需要对红黑树做任何调整，它仍是一棵红黑树；如果</span><span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">是黑色的，所有经过</span><span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">的简单路径上都会少一个黑色结点数，所以需要进行调整。</span></p>
<p>&nbsp;&nbsp;&nbsp; 下面来根据不同情景分情况讨论，因为二叉树的情景左右颠倒时调整方式也可以左右颠倒，我们只讨论<span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">是左子结点的情况。设刚接替</span><span style="font-family:Times New Roman;">subst</span><span style="font-family:宋体;">的</span><span style="font-family:Times New Roman;">temp</span><span style="font-family:宋体;">为</span><span style="font-family:Times New Roman;">X</span><span style="font-family:宋体;">，</span><span style="font-family:Times New Roman;">X</span><span style="font-family:宋体;">的新右兄弟为</span><span style="font-family:Times New Roman;">W</span><span style="font-family:宋体;">。从经过简化的源码来看，关于结点颜色的变化很令人费解，我们不妨先来看一看：</span><br>&nbsp;&nbsp;&nbsp; ①W为红色：将W染黑，将X与W的父结点X-&gt;parent染红，X-&gt;parent左旋，W重设为X的新右兄弟，然后转入情景①、②或③；<br>&nbsp;&nbsp;&nbsp; ②W为黑色，W两个后继都是黑色：将W染红，X重设为X-&gt;parent；<br>&nbsp;&nbsp;&nbsp; ③W为黑色，W右子结点为黑色：将W左子结点染黑，将W染红，W右旋，W重设为X的新右兄弟，然后将X-&gt;parent的颜色赋给W，将X-&gt;parent染黑，X-&gt;parent左旋，根赋给temp；<br>&nbsp;&nbsp;&nbsp; ④W为黑色，W右子结点为红色：将W左子结点染黑，将W染红，W右旋，W重设为X的新右兄弟，然后将X-&gt;parent的颜色赋给W，将X-&gt;parent染黑，将W右子结点染黑，X-&gt;parent左旋，根赋给temp。</p>
<p>&nbsp;&nbsp;&nbsp; 最后还要把temp染黑。我们可以看到情景①中进行了一次左旋，情景②只进行了染色，情景③、④都进行了一次右旋和一次左旋。情景①处理结束时一定还要转入别的情景，情景②、③、④的出现则标志着本次调整的结束。那么，红黑树删除结点后的调整过程中，依情景①循环出现的次数，调整过程中旋转的最多见的次数将是1次、2次、3次，再往上次数越多越罕见（依情景①循环出现的次数），最多旋转次数将可能到达树高即log2n次。生产环境中，删除结点后平均每次调整中旋转的次数就像分析源码之前提到的，将是常数规模的。</p>
<p>&nbsp;&nbsp;&nbsp; 接下来我打算以逐步翻新版本的方式重写红黑树，更精细、直观地了解红黑树这一数据结构。而在重写之前，我们需要了解，nginx的红黑中所有的叶结点，都是哨兵（sentinel），这在调整红黑树时达成了对红黑树的一种优化。通过增加一层全黑的子结点，红黑树中实际有值的子树里，就允许在子结点出现红色结点了。虽然我没有证明，但这常数规模地增加了删除结点时的旋转次数，也促进了插入新结点时进行调整的概率（增加了在红色结点下插入新结点的概率），同样增加了旋转的次数。而旋转将压缩红黑树子树的高度，提高查询效率。</p>
<p>&nbsp;&nbsp;&nbsp; 在由朴素到精致地重写红黑树的过程中，我将由少到多地考虑使用nginx对红黑树的优化，或者加入我自己的优化。</p>
<p>&nbsp;&nbsp;&nbsp; <span style="color:#FF0000;">从杭州回来后翻了CLRS（算法导论），发现：<br>&nbsp;&nbsp;&nbsp; 首先，nginx的红黑树中，sentinel结点并非独创的优化手段，CLRS的红黑树也是带哨兵的，可以说，一般的，我们令红黑树带哨兵。目的是更直截了当的满足红黑树的叶结点全黑约束，同时更方便标识树的边缘。</span></p>
<p>&nbsp;&nbsp;&nbsp; <span style="color:#FF0000;">其次，所有的叶结点都是由同一个哨兵结点代表，节省了空间开销，省去了叶结点逐一染色的麻烦。</span></p>
<p>&nbsp;&nbsp;&nbsp; <span style="color:#FF0000;">另外，之前我感到迷惑的static inline组合用法，在oschina.net获得了解释：<br>&nbsp;&nbsp;&nbsp; 1.inline函数是不能像传统的函数那样放在.c中然后在.h中给出接口在其余文件中调用的,因为inline函数其实是跟宏定义类似，被调用时尝试在调用处直接展开整个函数体，不存在所谓的函数入口。<br>&nbsp;&nbsp;&nbsp; 2.因为第一点，会出现一个问题，如果inline函数在两个不同的文件中出现。也就是说一个头文件被两个不同的源文件包含，则会出现重名，链接失败。static inline 的用法就能很好的解决这个问题。使用static修饰符，函数仅在文件内部可见，不会污染命名空间。可以理解为一个inline函数在不同的源文件里面生成了不同的实例，而且名字是完全相同的 。</span></p>
<p>&nbsp;&nbsp;&nbsp; <span style="color:#FF0000;">总结一下。功能上，我们需要微型函数被大量调用时尝试内联展开以节省压栈弹栈的开销；实践中，为了防止不同文件中函数同名时的链接错误，我们需要加上static关键字的限制。（尽管inline关键字的效果有所不同，c99标准和gcc下static inline组合是兼容的，效果相同）</span></p>
<p>&nbsp;&nbsp;&nbsp; <span style="color:#FF0000;">之前看到nginx源码中函数参数有双重指针一直很费解，今天研究了一下才发现原因。ngx_rbtree_t中，root经常使用双重结点指针，也就是根结点地址的地址。如果树的修改过程中，根结点地址被别的结点地址替换掉，需要重新设置根的地址<em>root。假设ngx_rbtree_t中的根地址参数是</em>root单层指针，进入函数体时将是一个值传递，出函数体时无论函数体中如何更改根的地址，都是无效的，只有对根结点内容的修改能保留下来。所以要么使用双重指针作为根地址的参数，要么提供树结构体的地址，变相提供双重指针作为参数，当然可以提供树的结构体对象本身作为参数，但是值传递是要复制整个值对象的，显然当结构体比较大时这样做将明显增加开销。nginx选择双重指针而非结构体指针来避免树结构体内的变量遍历寻址，进一步提高效率。</span></p>
]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 就来挑两个特别的数据结构ngx_int_t、ngx_rbtree_t作为研读nginx源码的开始吧。<br>]]>
    
    </summary>
    
      <category term="C" scheme="http://blog.bipedalbit.net/tags/C/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/tags/nginx/"/>
    
      <category term="ngx_int_t" scheme="http://blog.bipedalbit.net/tags/ngx-int-t/"/>
    
      <category term="rbtree" scheme="http://blog.bipedalbit.net/tags/rbtree/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/categories/nginx/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[【二】nginx源码文件结构浏览 ]]></title>
    <link href="http://blog.bipedalbit.net/2015/7/23/%E3%80%90%E4%BA%8C%E3%80%91nginx%E6%BA%90%E7%A0%81%E6%96%87%E4%BB%B6%E7%BB%93%E6%9E%84%E6%B5%8F%E8%A7%88/"/>
    <id>http://blog.bipedalbit.net/2015/7/23/【二】nginx源码文件结构浏览/</id>
    <published>2015-07-23T07:01:33.000Z</published>
    <updated>2015-09-15T14:10:12.419Z</updated>
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp; 要读大项目的源码，最好还是先把项目文件组织结构弄清楚，这样后面找源码会比较方便，对整体项目的架构也能有一个具体一些的概念。<br><a id="more"></a><br>&nbsp;&nbsp;&nbsp; 我下载的<span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">源码版本不可考，按日期来看是</span><span style="font-family:Times New Roman;">2015</span><span style="font-family:宋体;">年</span><span style="font-family:Times New Roman;">7</span><span style="font-family:宋体;">月的最新版本。解压包获得源码文件夹，进入根目录发现有几个子目录：</span><span style="font-family:Times New Roman;">auto</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">conf</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">contrib</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">docs</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">misc</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">src</span><span style="font-family:宋体;">。从字面上猜测，是自动机或脚本（</span><span style="font-family:Times New Roman;">auto&nbsp;script</span><span style="font-family:宋体;">）、配置文件（</span><span style="font-family:Times New Roman;">configuration</span><span style="font-family:宋体;">）、支持文件（</span><span style="font-family:Times New Roman;">contributions</span><span style="font-family:宋体;">）、文档（</span><span style="font-family:Times New Roman;">document</span><span style="font-family:宋体;">）、杂项（</span><span style="font-family:Times New Roman;">miscellaneous</span><span style="font-family:宋体;">）、源码（</span><span style="font-family:Times New Roman;">source&nbsp;code</span><span style="font-family:宋体;">）。</span></p>
<h3 id="1、浏览源码文件夹根目录">1<span style="font-family:宋体;">、浏览源码文件夹根目录</span></h3><p>&nbsp;&nbsp;&nbsp; 按照惯例，我选择先看看文档，但是<span style="font-family:Times New Roman;">doc</span><span style="font-family:宋体;">子目录里并没有详细的离线文档，只有</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">官网的链接和一些看不懂的小文件。</span></p>
<p>&nbsp;&nbsp;&nbsp; 杂项<span style="font-family:Times New Roman;">misc</span><span style="font-family:宋体;">里也只有看不太懂的一个</span><span style="font-family:Times New Roman;">GNUmakefile</span><span style="font-family:宋体;">和一个</span><span style="font-family:Times New Roman;">README</span><span style="font-family:宋体;">文件。</span></p>
<p>&nbsp;&nbsp;&nbsp; contrib<span style="font-family:宋体;">子目录里有一个</span><span style="font-family:Times New Roman;">geo2nginx.pl</span><span style="font-family:宋体;">（</span><span style="font-family:Times New Roman;">geo</span><span style="font-family:宋体;">模块的</span><span style="font-family:Times New Roman;">CSV</span><span style="font-family:宋体;">数据库格式转换</span><span style="font-family:Times New Roman;">perl</span><span style="font-family:宋体;">脚本），一套编码转换文件（包括两个编码映射表和一个编码转换</span><span style="font-family:Times New Roman;">perl</span><span style="font-family:宋体;">脚本），和</span><span style="font-family:Times New Roman;">vim</span><span style="font-family:宋体;">的</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">专用语法高亮配置文件。</span></p>
<p>&nbsp;&nbsp;&nbsp; conf<span style="font-family:宋体;">子目录里有几个编码映射表、变量映射表和一个服务器配置文件。</span></p>
<p>&nbsp;&nbsp;&nbsp; auto<span style="font-family:宋体;">子目录里有包括服务器安装脚本在内的许多脚本，现在看脚本还比较吃力，所以只简单浏览一下。我们发现</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">支持很多主流的操作系统平台：苹果的开源系统</span><span style="font-family:Times New Roman;">darwin</span><span style="font-family:宋体;">、重要的</span><span style="font-family:Times New Roman;">Unix</span><span style="font-family:宋体;">分支</span><span style="font-family:Times New Roman;">freebsd</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">linux</span><span style="font-family:宋体;">、大多数版本已经开源的又一个</span><span style="font-family:Times New Roman;">Unix</span><span style="font-family:宋体;">分支</span><span style="font-family:Times New Roman;">solaris</span><span style="font-family:宋体;">、用户占有率最高的</span><span style="font-family:Times New Roman;">win32</span><span style="font-family:宋体;">。</span></p>
<p>&nbsp;&nbsp;&nbsp; src<span style="font-family:宋体;">子目录果然都是需要研究的</span><span style="font-family:Times New Roman;">nginx</span><span style="font-family:宋体;">的</span><span style="font-family:Times New Roman;">C</span><span style="font-family:宋体;">源码，下面我们会深入浏览。</span></p>
<h3 id="2、浏览src子目录">2<span style="font-family:宋体;">、浏览</span><span style="font-family:Times New Roman;">src</span><span style="font-family:宋体;">子目录</span></h3><p>&nbsp;&nbsp;&nbsp; 还是来看一下<span style="font-family:Times New Roman;">src</span><span style="font-family:宋体;">子目录下有哪些子目录：</span><span style="font-family:Times New Roman;">core</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">event</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">http</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">mail</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">misc</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">mysql</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">os</span><span style="font-family:宋体;">、</span><span style="font-family:Times New Roman;">stream</span><span style="font-family:宋体;">。还是来按照字面意思猜测一下：核心（主函数、数据结构、主要方法）、事件（事件处理机制）、超文本传输协议（</span><span style="font-family:Times New Roman;">http</span><span style="font-family:宋体;">相关）、邮件（邮件相关）、杂项（</span><span style="font-family:Times New Roman;">google&nbsp;perftools</span><span style="font-family:宋体;">相关源码）、</span><span style="font-family:Times New Roman;">mysql</span><span style="font-family:宋体;">数据库、操作系统（</span><span style="font-family:Times New Roman;">Unix</span><span style="font-family:宋体;">和</span><span style="font-family:Times New Roman;">Win32</span><span style="font-family:宋体;">两类系统的系统调用程序源码）、流（意义不明）。看起来</span><span style="font-family:Times New Roman;">./src/core</span><span style="font-family:宋体;">子目录对研究源码来说是一个不错的开始。</span></p>
<p align="center"><br>    <span style="font-family:宋体;"><img src="http://img.blog.csdn.net/20150723165913000?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQv/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/Center" alt=""></span><br>    图<span style="font-family:Times New Roman;">2&nbsp;nginx</span><span style="font-family:宋体;">源码文件结构图</span><br></p>]]></content>
    <summary type="html">
    <![CDATA[<p>&nbsp;&nbsp;&nbsp; 要读大项目的源码，最好还是先把项目文件组织结构弄清楚，这样后面找源码会比较方便，对整体项目的架构也能有一个具体一些的概念。<br>]]>
    
    </summary>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/tags/nginx/"/>
    
      <category term="nginx" scheme="http://blog.bipedalbit.net/categories/nginx/"/>
    
  </entry>
  
</feed>
